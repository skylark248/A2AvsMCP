---
phase: 11-tool-discovery-scenario
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/lib/trace/JsonTree.tsx
  - frontend/src/components/traces/ProtocolEnvelopeDrawer.tsx
autonomous: true
requirements: [DISC-02]
tags: [refactor, frontend, mui]

must_haves:
  truths:
    - "ProtocolEnvelopeDrawer continues to render `tool_discovery` payloads identically (no visual regression)"
    - "Both ProtocolEnvelopeDrawer AND any future consumer can import JsonTree as a named export from a shared module"
    - "All existing frontend tests still pass after the move"
  artifacts:
    - path: "frontend/src/lib/trace/JsonTree.tsx"
      provides: "Shared JsonTree component, FIELD_ANNOTATIONS map, annotate() helper"
      exports: ["JsonTree", "FIELD_ANNOTATIONS", "annotate"]
    - path: "frontend/src/components/traces/ProtocolEnvelopeDrawer.tsx"
      provides: "Drawer that imports JsonTree from new location instead of declaring it inline"
      contains: 'import { JsonTree } from "../../lib/trace/JsonTree"'
  key_links:
    - from: "frontend/src/components/traces/ProtocolEnvelopeDrawer.tsx"
      to: "frontend/src/lib/trace/JsonTree.tsx"
      via: "named import"
      pattern: 'from "\\.\\./\\.\\./lib/trace/JsonTree"'
---

<objective>
Extract `JsonTree`, `FIELD_ANNOTATIONS`, and `annotate()` from `frontend/src/components/traces/ProtocolEnvelopeDrawer.tsx` into a new shared module `frontend/src/lib/trace/JsonTree.tsx`, then update the drawer to import them. This is a Wave 0 refactor that ships green so Wave 1's `DiscoveryPhasePanel` can import the same renderer without duplicating it.

Purpose: UI-SPEC line 145 explicitly forbids duplicating JsonTree inside DiscoveryPhasePanel ("extract, do not duplicate"). RESEARCH Pitfall #5 flags that the drawer's import path MUST be updated in the same plan or all trace pages break.

Output: New file `lib/trace/JsonTree.tsx` with three named exports; drawer cleaned up to consume them.

Honors: D-72 (single panel above both columns will need the shared renderer).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tool-discovery-scenario/11-CONTEXT.md
@.planning/phases/11-tool-discovery-scenario/11-RESEARCH.md
@.planning/phases/11-tool-discovery-scenario/11-PATTERNS.md
@.planning/phases/11-tool-discovery-scenario/11-UI-SPEC.md

<interfaces>
<!-- Current location of JsonTree (will be MOVED, not copied). -->
<!-- Source: frontend/src/components/traces/ProtocolEnvelopeDrawer.tsx -->

Lines 26-45 — FIELD_ANNOTATIONS:
```typescript
const FIELD_ANNOTATIONS: Record<string, string> = { /* ~20 entries */ };
```

Lines 47-50 — annotate():
```typescript
function annotate(key: string, parentKey?: string): string | undefined { /* ~3 lines */ }
```

Lines 80-155 — JsonTree (recursive component):
```typescript
function JsonTree(props: { data: unknown; depth?: number; parentKey?: string }) { /* ~75 lines */ }
```

After this plan ships, all three become named exports of `frontend/src/lib/trace/JsonTree.tsx`.

Drawer's existing usage at line 241: `<JsonTree data={data} />` — UNCHANGED, only the import source changes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create frontend/src/lib/trace/JsonTree.tsx with three named exports moved verbatim from ProtocolEnvelopeDrawer</name>
  <files>frontend/src/lib/trace/JsonTree.tsx</files>
  <read_first>
    - frontend/src/components/traces/ProtocolEnvelopeDrawer.tsx (FULL FILE — you are extracting from it)
    - frontend/src/lib/trace/eventColors.ts (sibling file — confirm directory exists, mirror import style)
    - .planning/phases/11-tool-discovery-scenario/11-PATTERNS.md (lines 25-45 — extraction contract)
    - .planning/phases/11-tool-discovery-scenario/11-RESEARCH.md (Pitfall #5, lines 291-296)
    - .planning/phases/11-tool-discovery-scenario/11-UI-SPEC.md (line 145 — "extract, do not duplicate")
  </read_first>
  <action>
Create new file `frontend/src/lib/trace/JsonTree.tsx`. The file MUST contain exactly these named exports, moved VERBATIM from `frontend/src/components/traces/ProtocolEnvelopeDrawer.tsx`:

1. `export const FIELD_ANNOTATIONS: Record<string, string>` — copy lines 26-45 verbatim, change `const` to `export const`.
2. `export function annotate(key: string, parentKey?: string): string | undefined` — copy lines 47-50 verbatim, prefix with `export`.
3. `export function JsonTree(props: { data: unknown; depth?: number; parentKey?: string })` — copy lines 80-155 verbatim, prefix with `export`.

Required imports at top of new file (only what JsonTree + annotate actually use — match the drawer's current usage):
```typescript
import { Box, Tooltip } from "@mui/material";
```

DO NOT add any other imports. DO NOT modify the function bodies. DO NOT add JSDoc beyond what's already in the source. The whole point is byte-for-byte equivalence so React renders are identical.

After writing, verify the file ends with a newline. No `default` export — three named exports only.

Per UI-SPEC line 145 and RESEARCH Pitfall #5, this extraction is what unblocks Wave 1's DiscoveryPhasePanel from rendering tool inputSchema and agent-card payloads with the same field-annotation tooltips the drawer uses.
  </action>
  <verify>
    <automated>test -f frontend/src/lib/trace/JsonTree.tsx &amp;&amp; grep -c "^export " frontend/src/lib/trace/JsonTree.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/trace/JsonTree.tsx` exists.
    - `grep -E "^export (const FIELD_ANNOTATIONS|function annotate|function JsonTree)" frontend/src/lib/trace/JsonTree.tsx | wc -l` returns 3.
    - `grep -c 'import.*from "@mui/material"' frontend/src/lib/trace/JsonTree.tsx` returns at least 1 (Box, Tooltip imported).
    - `grep -c "default" frontend/src/lib/trace/JsonTree.tsx | head -1` — no `export default` line present.
    - File contains the literal string `FIELD_ANNOTATIONS` and the literal string `function JsonTree`.
  </acceptance_criteria>
  <done>New file exists with the three required named exports moved verbatim from the drawer; ready for the drawer to import.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ProtocolEnvelopeDrawer.tsx to import JsonTree from the new module and delete the inline definitions</name>
  <files>frontend/src/components/traces/ProtocolEnvelopeDrawer.tsx</files>
  <read_first>
    - frontend/src/components/traces/ProtocolEnvelopeDrawer.tsx (FULL FILE — you are removing lines 26-50 and 80-155 from it)
    - frontend/src/lib/trace/JsonTree.tsx (the file you just created in Task 1 — confirm exports)
    - .planning/phases/11-tool-discovery-scenario/11-PATTERNS.md (lines 47-56 — drawer modification contract)
    - .planning/phases/11-tool-discovery-scenario/11-RESEARCH.md (Pitfall #5, lines 291-296 — must update import or build breaks)
  </read_first>
  <action>
Modify `frontend/src/components/traces/ProtocolEnvelopeDrawer.tsx`:

1. DELETE the inline `FIELD_ANNOTATIONS` constant (currently lines 26-45).
2. DELETE the inline `annotate()` function (currently lines 47-50).
3. DELETE the inline `JsonTree` function declaration (currently lines 80-155).
4. ADD this named import at the top of the file (alongside the existing imports — pick the alphabetical-correct spot among other relative imports):
```typescript
import { JsonTree } from "../../lib/trace/JsonTree";
```
5. The existing usage site `<JsonTree data={data} />` at line 241 stays UNCHANGED — only the source of the symbol changes.
6. If `Tooltip` import on the existing top-of-file `@mui/material` import line is now unused (it was used inside JsonTree), remove only `Tooltip` from that import (keep all other named imports). Run `grep "Tooltip" frontend/src/components/traces/ProtocolEnvelopeDrawer.tsx` after the edit; if zero hits, the removal was correct. If hits remain elsewhere in the drawer body, leave Tooltip in.

DO NOT introduce `import * from`. DO NOT add a default export. DO NOT touch the JSX bodies of `ProtocolEnvelopeDrawer` itself — only the helper declarations are leaving.

This satisfies RESEARCH Pitfall #5 — the drawer's import path is updated in the same plan as the move, so `npm test` stays green.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^function JsonTree" frontend/src/components/traces/ProtocolEnvelopeDrawer.tsx` returns 0 (inline definition removed).
    - `grep -c "^const FIELD_ANNOTATIONS" frontend/src/components/traces/ProtocolEnvelopeDrawer.tsx` returns 0 (inline annotations removed).
    - `grep -c 'from "../../lib/trace/JsonTree"' frontend/src/components/traces/ProtocolEnvelopeDrawer.tsx` returns 1 (named import added).
    - `grep -c "<JsonTree" frontend/src/components/traces/ProtocolEnvelopeDrawer.tsx` returns at least 1 (usage site preserved).
    - `cd frontend && npm test -- --run` exits 0 — full vitest suite (286+ tests baseline) still green.
  </acceptance_criteria>
  <done>Drawer imports JsonTree from the new shared module; inline copies removed; full frontend test suite passes; refactor ships without behavioral change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Refactor-only plan — moves code between files within the same trust zone (frontend client). No new boundaries crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | `JsonTree` rendering of tool/agent payload fields | accept | React's default text-escaping mitigates XSS in JSX text nodes. The extraction is byte-for-byte; no `dangerouslySetInnerHTML` is introduced. Acceptance criterion `grep -c "dangerouslySetInnerHTML" frontend/src/lib/trace/JsonTree.tsx` returns 0 (verified by code review during Task 1). |
| T-11-01-02 | Information Disclosure | Refactor leaks no new data | accept | No new event types consumed; same payload fields rendered with same logic. |
</threat_model>

<verification>
- `cd frontend && npm test -- --run` — full vitest suite green (no regression).
- New file exists with three named exports.
- Drawer imports from new module; inline copies deleted.
- No new `dangerouslySetInnerHTML` introduced (T-11-01-01).
</verification>

<success_criteria>
- DISC-02 partial coverage: shared JsonTree primitive exists for DiscoveryPhasePanel to import in Wave 1.
- Refactor-only plan ships green — zero behavioral change to ProtocolEnvelopeDrawer.
- Both `frontend/src/lib/trace/JsonTree.tsx` and the modified `ProtocolEnvelopeDrawer.tsx` committed.
</success_criteria>

<output>
After completion, create `.planning/phases/11-tool-discovery-scenario/11-01-SUMMARY.md`.

Then run `graphify update .` (best-effort per CLAUDE.md graphify rule — keeps the AST graph current after code-file modifications). If the `graphify` CLI is not on PATH, skip with a one-line note in the SUMMARY; this is non-blocking.
</output>
