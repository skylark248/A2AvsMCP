---
phase: 15-verification-and-cleanup
plan: 04
type: execute
wave: 1
depends_on: [15-03]
files_modified: ["frontend/src/components/traces/TraceExplorer.tsx"]
autonomous: true
requirements: []
must_haves:
  truths:
    - "ProtocolTier function signature no longer accepts pinnedEventId parameter"
    - "Call site at line 293 removes pinnedEventId argument"
    - "No references to pinnedEventId remain in ProtocolTier function body"
    - "All frontend tests pass after cleanup"
  artifacts:
    - path: "frontend/src/components/traces/TraceExplorer.tsx"
      provides: "ProtocolTier function with clean signature (line 322) and call site (line 293)"
      contains: "function ProtocolTier.*events.*TraceEvent"
  key_links:
    - from: "TraceExplorer.tsx line 293 (call site)"
      to: "TraceExplorer.tsx line 322 (ProtocolTier signature)"
      via: "function argument passing"
      pattern: "<ProtocolTier events.*>"
---

<objective>
**Remove dead pinnedEventId prop from ProtocolTier:** The `pinnedEventId` parameter is accepted in the ProtocolTier function signature but never used in the function body. Remove both the parameter declaration and the argument at the call site.

**Purpose:** Closes W-VERIF-3 code quality gap found during v2.0 audit. Dead props create confusion for maintainers and unnecessary re-renders.

**Output:** TraceExplorer.tsx with ProtocolTier signature cleaned (line 322) and call site updated (line 293).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-verification-and-cleanup/15-RESEARCH.md (lines 248-276: dead prop analysis)
</context>

<tasks>

<task type="auto">
  <name>Task: Remove pinnedEventId from ProtocolTier signature and call site</name>
  <files>frontend/src/components/traces/TraceExplorer.tsx</files>
  <action>
Locate two sites in TraceExplorer.tsx:

**Site 1 - Call site (line 293):**
Current code:
```typescript
<ProtocolTier events={filteredEvents} pinnedEventId={pinnedEventId} />
```

Change to:
```typescript
<ProtocolTier events={filteredEvents} />
```

**Site 2 - Function signature (line 322):**
Current code:
```typescript
function ProtocolTier({ events, pinnedEventId }: { events: TraceEvent[]; pinnedEventId?: string | null }) {
```

Change to:
```typescript
function ProtocolTier({ events }: { events: TraceEvent[] }) {
```

Verification:
1. Search for "pinnedEventId" in TraceExplorer.tsx (should find only uses in TraceExplorer component state, not in ProtocolTier)
2. The state declaration `const [pinnedEventId, setPinnedEventId]` at line 60 should remain (used for sequence diagram, not ProtocolTier)
3. Uses of pinnedEventId in lines 82, 87, 98 (for scroll-to-pin effect in sequence diagram) should remain unchanged
4. ProtocolTier call at line 293 should have argument removed
5. ProtocolTier function body (lines 323-379) should remain unchanged (never referenced pinnedEventId anyway)

Why this is safe:
- pinnedEventId was in ProtocolTier signature but never accessed in function body (verified in 15-RESEARCH.md lines 254-268)
- pinnedEventId is used elsewhere in TraceExplorer component (lines 82, 87, 98) for scroll-to-pin behavior in sequence diagram view, but that uses a different code path
- ProtocolTier only renders when viewMode="list" (lines 292-293) and never uses the prop
- Removal is pure cleanup, no functional impact
  </action>
  <verify>
    <automated>grep -n "ProtocolTier.*pinnedEventId" /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP/frontend/src/components/traces/TraceExplorer.tsx && echo "FAIL: pinnedEventId still in ProtocolTier" || echo "PASS: pinnedEventId removed from ProtocolTier"; cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP/frontend && npm test 2>&1 | tail -3</automated>
  </verify>
  <done>
- TraceExplorer.tsx line 322 (ProtocolTier signature): pinnedEventId parameter removed
- TraceExplorer.tsx line 293 (call site): pinnedEventId argument removed
- No references to pinnedEventId in ProtocolTier function remain
- pinnedEventId state (line 60) in TraceExplorer component unchanged (used for sequence diagram)
- All frontend tests pass
- Code committed to git
  </done>
</task>

</tasks>

<threat_model>
Not applicable — Task is dead code removal with no security implications. No auth, validation, or data handling changed.
</threat_model>

<verification>
1. Grep verification: No ProtocolTier function should reference pinnedEventId
```bash
grep -A 60 "^function ProtocolTier" frontend/src/components/traces/TraceExplorer.tsx | grep pinnedEventId
```
Expected: No output (pinnedEventId should not appear in function body).

2. Call site verification: ProtocolTier call should not pass pinnedEventId
```bash
grep "ProtocolTier.*pinnedEventId" frontend/src/components/traces/TraceExplorer.tsx
```
Expected: No output (prop should be removed).

3. Test verification: Full frontend suite should pass
```bash
cd frontend && npm test 2>&1 | tail -10
```
Expected: All tests pass, no failures.
</verification>

<success_criteria>
- [ ] ProtocolTier function signature (line 322) removes pinnedEventId parameter
- [ ] ProtocolTier call site (line 293) removes pinnedEventId argument
- [ ] No grep matches for "ProtocolTier.*pinnedEventId"
- [ ] pinnedEventId state variable (line 60) in TraceExplorer component remains unchanged
- [ ] All frontend tests pass (npm test)
- [ ] Code committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/15-verification-and-cleanup/15-04-SUMMARY.md` documenting:
- File modified: frontend/src/components/traces/TraceExplorer.tsx (lines 293, 322)
- Changes: ProtocolTier signature cleaned, call site updated (2 sites)
- Test results: npm test pass/fail summary
- Commits: git log of this plan's commit
- W-VERIF-3 gap closure verified
</output>
