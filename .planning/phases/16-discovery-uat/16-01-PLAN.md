---
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [DISC-01, DISC-02]
---

# Phase 16-01: Live UAT for Tool Discovery Scenario

**Objective:** Run the `tool_discovery` scenario live on MCP, A2A, and compare modes via the UI. Verify the 4 human items from Phase 11 VERIFICATION.md: (1) A2A protocol flow + agent cards, (2) MCP protocol flow + stale-cache warning, (3) compare-mode D-72 single-panel layout, (4) visual ordering above execution events. Document findings in 11-VERIFICATION.md.

## Tasks

### Task 1: Set up local dev server and run tool_discovery MCP mode

<read_first>
- `.planning/phases/11-tool-discovery-scenario/11-VERIFICATION.md` — the 4 human items and expected behaviors
- `.planning/ROADMAP.md` § "Phase 16: Discovery UAT" — success criteria lines 216-221
- `src/a2a_vs_mcp/data/seeds/scenarios.json` — locate TICKET-1013 row to confirm tool_discovery scenario exists
- `frontend/src/components/traces/DiscoveryPhasePanel.tsx` lines 1-50 — component structure
- `frontend/src/features/traces/TraceWorkspacePage.tsx` lines 390-410 — mount site and gate logic
</read_first>

<action>
Start the development environment and run the demo on MCP protocol with the tool_discovery scenario:

1. In terminal 1, start backend: `cd /path/to/A2AvsMCP && python serve_ui.py` (or equivalent startup; opens port 8008)
2. In terminal 2, start frontend dev server: `cd frontend && npm run dev` (opens port typically 5173)
3. Navigate to `http://localhost:5173` (or the frontend dev URL shown by npm)
4. Open the demo interface and select:
   - Protocol: **MCP**
   - Scenario: **tool_discovery** (TICKET-1013 — the new scenario with unknown SKU)
5. Run the scenario to completion and observe:
   - MCP tool catalog renders in `DiscoveryPhasePanel` (above TraceExplorer)
   - Unknown product `NebulaSync Hub` triggers fallback (search_docs tool renders in executed tools)
   - If the MCP server's cached tools differ from requested_transport, a **warning icon** (`aria-label="Stale capability cache"`) appears on a tool card
   - The panel renders **above** the trace timeline (execution-phase events start below)
   - Record a screenshot or note key observations (timestamps, tool names, warning icon presence)
</action>

<acceptance_criteria>
- DiscoveryPhasePanel renders on TraceWorkspacePage for MCP run with tool_discovery scenario
- MCP tool catalog visible (tool names and timestamps present)
- search_docs tool appears in executed tools (fallback triggered by unknown SKU)
- If tool transport differs from requested: warning icon visible with aria-label="Stale capability cache"
- Panel renders above TraceExplorer (visual ordering confirmed)
- No console errors during scenario run
</acceptance_criteria>

### Task 2: Run tool_discovery A2A mode and verify agent-card column

<read_first>
- `.planning/phases/11-tool-discovery-scenario/11-VERIFICATION.md` lines 8-10 — human item #1 (A2A protocol path)
- `frontend/src/components/traces/DiscoveryPhasePanel.tsx` lines 126-207 — `renderA2aAgentCards` logic
- `src/a2a_vs_mcp/a2a/remote_broker.py` lines 236-243 — A2A `a2a_remote_discovery` emit
</read_first>

<action>
Run the same tool_discovery scenario on the A2A protocol:

1. In the demo interface, select:
   - Protocol: **A2A** (not MCP)
   - Scenario: **tool_discovery**
2. Run the scenario to completion and observe:
   - `DiscoveryPhasePanel` populates the A2A column (right side) with agent cards
   - Each agent card shows: agent name, timestamp (relative, e.g., `+42ms`)
   - Agent cards display associated skills/tools as chips below the agent name
   - The MCP column (left) shows placeholder text: `"Run on MCP to populate"` (static, no interaction)
   - No error messages in console
   - Panel remains above TraceExplorer
3. Record observations about agent-card rendering, skill visibility, and placeholder text
</action>

<acceptance_criteria>
- DiscoveryPhasePanel renders on TraceWorkspacePage for A2A run with tool_discovery scenario
- A2A column populates with at least one agent card (agent name visible)
- Agent card timestamps render (relative format, e.g., `+42ms`)
- Skills/tools render as chips under agent name
- MCP column shows placeholder text: `"Run on MCP to populate"` (non-interactive)
- No console errors during scenario run
</acceptance_criteria>

### Task 3: Run tool_discovery in compare mode (MCP vs A2A) and verify D-72 layout

<read_first>
- `.planning/phases/11-tool-discovery-scenario/11-VERIFICATION.md` lines 14-16 — human item #3 (compare-mode D-72 single-panel contract)
- `.planning/phases/11-tool-discovery-scenario/11-CONTEXT.md` lines 39 — D-72 decision: "one DiscoveryPhasePanel above both columns"
- `frontend/src/features/compare/CompareTracesPanel.tsx` lines 49-121 — mount site and dual-column Grid
</read_first>

<action>
Run the tool_discovery scenario on **both** MCP and A2A protocols, then compare the two runs side-by-side:

1. Complete a MCP tool_discovery run (Task 1 output); note its `run_id` or export trace
2. Complete an A2A tool_discovery run (Task 2 output); note its `run_id` or export trace
3. Navigate to the **Compare Traces** interface and load both runs
4. Observe the layout:
   - **Exactly ONE `DiscoveryPhasePanel`** appears ABOVE the dual-column `TraceExplorer` Grid
   - Panel is full-width (spans both MCP and A2A columns internally)
   - The panel's internal layout splits MCP tool catalog (left) and A2A agent cards (right)
   - Both columns are populated (no placeholders, since both protocols' data is present)
   - Dual-column `TraceExplorer` sits below the panel (execution-phase events visible separately for each protocol)
5. Confirm that comparing two **non-discovery** runs (e.g., two race runs) does NOT show the DiscoveryPhasePanel (presence gate working)
6. Record screenshot or observations about panel position and absence on non-discovery comparisons
</action>

<acceptance_criteria>
- CompareTracesPanel loads both MCP and A2A tool_discovery runs side-by-side
- Exactly ONE DiscoveryPhasePanel renders above dual-column Grid (not two panels)
- Panel is full-width (spans the layout width, not constrained to one column)
- MCP tool catalog and A2A agent cards both populate the panel (both columns have real data)
- Dual-column TraceExplorer Grid visible below the panel
- Comparing two non-discovery runs: DiscoveryPhasePanel does NOT appear (presence gate honored)
- No console errors
</acceptance_criteria>

### Task 4: Verify visual ordering and update Phase 11 VERIFICATION.md status

<read_first>
- `.planning/phases/11-tool-discovery-scenario/11-VERIFICATION.md` lines 17-19 — human item #4 (visual ordering)
- `.planning/ROADMAP.md` line 219 — success criterion #3: "panel renders strictly BEFORE any execution-phase events"
</read_first>

<action>
Confirm visual ordering in all three modes (single MCP, single A2A, compare) and update verification status:

1. **Single MCP run (Task 1):** Confirm the `DiscoveryPhasePanel` is positioned **above** the `TraceExplorer` block. Execution-phase events (tool calls, agent messages, etc.) start in the explorer **below** the panel. No tool_discovery events clutter the main execution timeline.
2. **Single A2A run (Task 2):** Repeat ordering check. Panel above, explorer below.
3. **Compare mode (Task 3):** Panel above both columns, dual explorer grid below.
4. **Update** `.planning/phases/11-tool-discovery-scenario/11-VERIFICATION.md`:
   - Change frontmatter `status:` from `human_needed` to `passed`
   - Add a `human_verification_evidence:` section below the frontmatter with:
     ```yaml
     human_verification_evidence:
       - test: "Run tool_discovery on A2A protocol via live UI"
         result: "✓ Passed — agent cards populated with [names], skills rendered as chips"
       - test: "Run tool_discovery on MCP protocol via live UI"
         result: "✓ Passed — tool catalog populated, stale-cache warning [present|absent], search_docs fallback visible"
       - test: "Run tool_discovery in compare mode (D-72 layout)"
         result: "✓ Passed — single full-width panel above dual-column explorer"
       - test: "Verify visual ordering (panel before execution events)"
         result: "✓ Passed — panel ABOVE explorer on all three modes"
     ```
   - Update the closing line to reflect verification date/time (e.g., `*Verified: 2026-05-04T16:30:00Z (human UAT completed)*`)
5. Commit the updated VERIFICATION.md file
</action>

<acceptance_criteria>
- DiscoveryPhasePanel visually positioned ABOVE TraceExplorer in all three modes (single MCP, single A2A, compare)
- Execution-phase events do NOT appear in the DiscoveryPhasePanel timeline
- 11-VERIFICATION.md frontmatter `status:` changed to `passed`
- `human_verification_evidence:` section added with results for all 4 tests
- File committed to git with message matching "docs(phase-16): complete Phase 11 human UAT"
</acceptance_criteria>

## must_haves

1. Tool discovery scenario (`tool_discovery` seed row) successfully executes on both MCP and A2A protocols via live UI — not just pytest fixtures
2. DiscoveryPhasePanel populates and renders correctly on all three observation points: TraceWorkspacePage (MCP mode), TraceWorkspacePage (A2A mode), CompareTracesPanel (both modes)
3. Stale-capability-cache warning appears when tool transport diverges (MCP mode)
4. Unknown-tool fallback story reads end-to-end: unknown SKU → search_docs fallback tool visible in executed tools (MCP mode)
5. D-72 contract (single full-width panel above dual-column Grid in compare mode) confirmed by live visual inspection
6. Visual ordering verified: panel above execution events on all modes
7. 11-VERIFICATION.md status updated to `passed` with timestamped human evidence

## Verification Criteria

- All 4 human items from Phase 11 VERIFICATION.md lines 8-19 verified on live UI
- ROADMAP success criteria (Phase 16, lines 216-221) met
- DISC-01 (tool_discovery scenario + MCP/A2A paths) and DISC-02 (panel layout and ordering) confirmed via live execution
- No regressions: other scenarios continue to load (tool_discovery gate does not break existing paths)
