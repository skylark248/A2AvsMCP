---
phase: 07-race-backend-lanes-harness-recovery
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/a2a_vs_mcp/race/failure.py
  - tests/race/test_inject_fault.py
  - pyproject.toml
autonomous: true
requirements: [RACE-03, RACE-07]
tags: [phase6-delta, dependency, exception-class]

must_haves:
  truths:
    - "race/failure.py exports InjectedFaultError (subclass of RuntimeError)"
    - "_apply_mutation raises InjectedFaultError (not bare RuntimeError) for RATE_LIMIT_429 and PARTIAL_COMMIT_5XX"
    - "tests/race/test_inject_fault.py asserts InjectedFaultError on raise paths (Phase 6 IRON-RULE atomicity test still green)"
    - "pyproject.toml [project] dependencies includes anthropic>=0.40 and pyyaml>=6.0"
    - "python -c 'import anthropic, yaml' exits 0 after editable reinstall"
  artifacts:
    - path: "src/a2a_vs_mcp/race/failure.py"
      provides: "InjectedFaultError class + updated _apply_mutation"
      contains: "class InjectedFaultError"
    - path: "pyproject.toml"
      provides: "anthropic + pyyaml direct dependencies"
      contains: "anthropic>=0.40"
  key_links:
    - from: "src/a2a_vs_mcp/race/failure.py::_apply_mutation"
      to: "src/a2a_vs_mcp/race/harness.py::TRANSIENT_RETRY_TYPES (added in Plan 10)"
      via: "exception class disambiguates injected from real Anthropic errors"
      pattern: "raise InjectedFaultError"

user_setup:
  - service: anthropic
    why: "Sonnet runner LLM + Haiku judge (D-42); race lanes are LLM-driven"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "https://console.anthropic.com/settings/keys"
    dashboard_config: []
---

<objective>
Wave 0 substrate. Land the Phase 6 delta required by Phase 7's harness retry classifier (per RESEARCH §2 — `InjectedFaultError(RuntimeError)`) and add two missing direct dependencies (`anthropic>=0.40` and explicit `pyyaml`) that the rest of Phase 7 imports from.

Purpose: The harness MUST distinguish `anthropic.RateLimitError` (real 429 — RETRY) from injected `FaultKind.RATE_LIMIT_429` (the test — NEVER RETRY). Phase 6 raises bare `RuntimeError` for both, so the classifier in Plan 10 cannot tell them apart. This plan changes the raise type so every downstream plan can use `except InjectedFaultError` cleanly.

Output: Updated `race/failure.py`, updated Phase 6 atomicity test, updated `pyproject.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-race-backend-lanes-harness-recovery/07-CONTEXT.md
@.planning/phases/07-race-backend-lanes-harness-recovery/07-RESEARCH.md
@.planning/phases/07-race-backend-lanes-harness-recovery/07-PATTERNS.md

<interfaces>
<!-- Existing race/failure.py shape (Phase 6) — modify in place. -->

```python
# src/a2a_vs_mcp/race/failure.py (Phase 6, lines 86-98 — current)
def _apply_mutation(kind: FaultKind, response: Any) -> Any:
    if kind is FaultKind.RATE_LIMIT_429:
        raise RuntimeError("HTTP 429 rate_limit (injected)")
    if kind is FaultKind.PARTIAL_COMMIT_5XX:
        raise RuntimeError("HTTP 503 partial_commit (injected)")
    return response
```

The Phase 6 atomicity test (`tests/race/test_inject_fault.py:33-42`) currently asserts `RuntimeError`. Update to `InjectedFaultError` for tighter contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add InjectedFaultError class and update _apply_mutation in race/failure.py</name>
  <read_first>
    - src/a2a_vs_mcp/race/failure.py (full file — 111 lines)
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-RESEARCH.md §2 (lines 270–292)
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-PATTERNS.md §"src/a2a_vs_mcp/race/failure.py — DELTA"
  </read_first>
  <action>
Edit `src/a2a_vs_mcp/race/failure.py`:

1. Insert this class verbatim immediately after the module imports block (before `class FaultKind`):

```python
class InjectedFaultError(RuntimeError):
    """Raised by _apply_mutation for RATE_LIMIT_429 and PARTIAL_COMMIT_5XX faults.

    Distinguishes injected faults from real Anthropic infra errors so the
    harness retry classifier never retries the test. Phase 7 D-38: harness
    retries anthropic.RateLimitError but NEVER InjectedFaultError.

    IS-A RuntimeError so existing Phase 6 callers that catch RuntimeError
    still work.
    """
```

2. Update `_apply_mutation` (currently lines 86-98) so the two raise sites use `InjectedFaultError` instead of `RuntimeError`:

```python
def _apply_mutation(kind: FaultKind, response: Any) -> Any:
    if kind is FaultKind.RATE_LIMIT_429:
        raise InjectedFaultError("HTTP 429 rate_limit (injected)")
    if kind is FaultKind.PARTIAL_COMMIT_5XX:
        raise InjectedFaultError("HTTP 503 partial_commit (injected)")
    return response
```

3. Add `InjectedFaultError` to the module's `__all__` if one exists; otherwise no change. Also add `from .failure import InjectedFaultError` re-export to `src/a2a_vs_mcp/race/__init__.py` so downstream plans can `from a2a_vs_mcp.race import InjectedFaultError`.

Do NOT modify `inject_fault()` (still emits the event BEFORE calling `_apply_mutation` — IRON RULE atomicity preserved). Do NOT modify `FaultKind` or `FailureScriptEntry`.
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && grep -n "class InjectedFaultError" src/a2a_vs_mcp/race/failure.py && grep -c "raise InjectedFaultError" src/a2a_vs_mcp/race/failure.py | grep -q "^2$" && python3 -c "from a2a_vs_mcp.race.failure import InjectedFaultError; assert issubclass(InjectedFaultError, RuntimeError)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class InjectedFaultError(RuntimeError):" src/a2a_vs_mcp/race/failure.py` returns exactly 1 match.
    - `grep -c "raise InjectedFaultError" src/a2a_vs_mcp/race/failure.py` outputs `2` (one for RATE_LIMIT_429, one for PARTIAL_COMMIT_5XX).
    - `grep -c "raise RuntimeError" src/a2a_vs_mcp/race/failure.py` outputs `0` (no bare RuntimeError raises remain in `_apply_mutation`).
    - `python3 -c "from a2a_vs_mcp.race import InjectedFaultError"` exits 0.
    - `python3 -c "from a2a_vs_mcp.race.failure import InjectedFaultError; assert issubclass(InjectedFaultError, RuntimeError)"` exits 0.
    - IRON RULE preserved: `grep -n "recorder.record" src/a2a_vs_mcp/race/failure.py` shows `record(...)` is called BEFORE `_apply_mutation` in `inject_fault()` body (lines 73-83 unchanged).
  </acceptance_criteria>
  <done>InjectedFaultError class exists, _apply_mutation raises it twice, race/__init__.py re-exports it, IRON RULE atomicity body unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update Phase 6 atomicity test to assert InjectedFaultError</name>
  <read_first>
    - tests/race/test_inject_fault.py (full file)
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-PATTERNS.md §"src/a2a_vs_mcp/race/failure.py — DELTA" final block
  </read_first>
  <action>
Edit `tests/race/test_inject_fault.py`:

1. Add `InjectedFaultError` to the existing import line:
   ```python
   from a2a_vs_mcp.race.failure import FaultKind, inject_fault, validate_failure_script, InjectedFaultError
   ```

2. Find every `assertRaises(RuntimeError)` site that exercises `RATE_LIMIT_429` or `PARTIAL_COMMIT_5XX` and change it to `assertRaises(InjectedFaultError)`. Per RESEARCH §"failure.py DELTA": "InjectedFaultError IS-A RuntimeError, so the existing test technically still passes, but assert the more specific type to lock the contract."

3. Do NOT change any test that doesn't involve these two raise-style faults. Do NOT change Pydantic validator tests (they don't call `_apply_mutation`).

4. Run `pytest tests/race/test_inject_fault.py -x` to confirm green.

If `tests/race/test_inject_fault.py` does not currently use `assertRaises(RuntimeError)` for these kinds (e.g., uses `assertRaises(Exception)` or pytest-style `pytest.raises`), still tighten to `InjectedFaultError`.
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && pytest tests/race/test_inject_fault.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "InjectedFaultError" tests/race/test_inject_fault.py` returns at least `1`.
    - `pytest tests/race/test_inject_fault.py -x -q` exits 0.
    - `pytest tests/race/ -x -q` (full Phase 6 race suite, 37 tests) exits 0 — no regression.
  </acceptance_criteria>
  <done>Phase 6 test green; Phase 7 contract locked.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add anthropic and pyyaml direct deps to pyproject.toml; verify import</name>
  <read_first>
    - pyproject.toml (full file — at minimum the [project] dependencies block)
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-RESEARCH.md §"Environment Availability" (lines 1271–1290)
  </read_first>
  <action>
1. Open `pyproject.toml`. Locate the `[project] dependencies = [ ... ]` array.

2. Add two entries to the array (preserve existing entries — append, do not replace):
   - `"anthropic>=0.40"`
   - `"pyyaml>=6.0"` (only if not already present — check first; it may be transitively pulled by fastapi/mcp but RESEARCH §Environment Availability says make it explicit because `task_config.yaml` is a v2.0 first-class file format)

3. Run `pip install -e .` (or the project's standard editable install command per CLAUDE.md `Commands` section) to resolve.

4. Verify importability:
   ```bash
   python3 -c "import anthropic; print(anthropic.__version__)"
   python3 -c "import yaml; print(yaml.__version__)"
   ```

Do NOT pin to an exact version — use `>=` so the minor-version drift doesn't break re-resolves. Do NOT add `anthropic` to optional/extras; it is a required direct dep because every race lane uses Sonnet.

Do NOT add `claude-haiku-4-5` or `claude-sonnet-4-6` as deps — those are model strings, not packages.
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && grep -E "anthropic>=" pyproject.toml && grep -E "pyyaml" pyproject.toml && python3 -c "import anthropic, yaml" && pytest tests/race/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "anthropic>=" pyproject.toml` outputs at least `1`.
    - `grep -ci "pyyaml" pyproject.toml` outputs at least `1`.
    - `python3 -c "import anthropic"` exits 0.
    - `python3 -c "import yaml"` exits 0.
    - `python3 -c "import anthropic; assert tuple(int(x) for x in anthropic.__version__.split('.')[:2]) >= (0, 40)"` exits 0.
    - `pytest tests/race/ -x -q` (Phase 6 + this plan's test changes) exits 0.
  </acceptance_criteria>
  <done>Both deps importable; full race suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness ↔ Anthropic API | API key (env var) crosses; transient network errors classified |
| race lane ↔ injected fault | Fault must NEVER be silently retried — disambiguation is correctness-critical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | `_apply_mutation` raise type | mitigate | Custom `InjectedFaultError(RuntimeError)` so harness retry classifier (Plan 10) cannot accidentally retry an injected fault — would invalidate the test |
| T-07-01-02 | Information Disclosure | `ANTHROPIC_API_KEY` env var | mitigate | Read from `os.getenv` only (Plan 06); no logging of the key value; `pyproject.toml` contains no secret |
| T-07-01-03 | Denial of Service | uncapped Anthropic concurrency once SDK lands | accept (deferred) | Mitigated structurally in Plan 10 (Semaphore(8)); this plan only adds the dep |
| T-07-01-04 | Spoofing | `anthropic` package supply chain | accept | Pinned `>=0.40` from PyPI; project trusts pip's TUF/index integrity |
</threat_model>

<verification>
- IRON RULE atomicity preserved: `inject_fault()` body still emits `recorder.record('fault_injected', ...)` BEFORE `_apply_mutation()` is invoked. Verify by `grep -B 2 -A 10 "Step 1 (record)" src/a2a_vs_mcp/race/failure.py` shows record-then-mutate ordering.
- All 37 Phase 6 race tests stay green (`pytest tests/race/ -x`).
- New deps importable; no version conflicts surfaced by `pip install -e .`.
</verification>

<success_criteria>
- `InjectedFaultError` exists, is raised twice in `_apply_mutation`, IS-A RuntimeError.
- `pyproject.toml` lists `anthropic>=0.40` and `pyyaml>=6.0` as direct dependencies.
- Phase 6 atomicity test asserts `InjectedFaultError` (tighter contract).
- Phase 6 full race suite remains green (no regression).
</success_criteria>

<output>
After completion, create `.planning/phases/07-race-backend-lanes-harness-recovery/07-01-SUMMARY.md`.
</output>
