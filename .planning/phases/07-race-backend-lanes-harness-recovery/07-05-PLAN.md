---
phase: 07-race-backend-lanes-harness-recovery
plan: 05
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/a2a_vs_mcp/race/metrics.py
autonomous: true
requirements: [RACE-04, RACE-06]
tags: [metrics, wasted-tokens, characteristic-event]

must_haves:
  truths:
    - "race/metrics.py defines compute_wasted_tokens(events, fault_id, lane) returning int — sums tokens_in+tokens_out for llm_call events with t_call_start_ms in [t_inject_ms, t_observed_ms] for the same lane (D-40)"
    - "race/metrics.py defines median_retries(events, fault_id, target) returning int (pure_mcp characteristic event count)"
    - "race/metrics.py defines median_delegations(events, fault_id) returning int (pure_a2a)"
    - "race/metrics.py defines median_switches(events, fault_id) returning int (hybrid)"
    - "race/metrics.py defines median_turns_after_fault(events, fault_id) returning int (fallback)"
    - "race/metrics.py defines aggregate_for_classifier(per_run_traces, task_id, lane) returning dict[str, Any] with keys recovery_rate, mean_wasted_tokens, mean_ttff_ms, characteristic_tool, median_retries / median_delegations / median_switches / median_turns_after_fault, lane_failed_reason"
    - "All functions are module-level (no class) — match evidence.py idiom"
  artifacts:
    - path: "src/a2a_vs_mcp/race/metrics.py"
      provides: "Pure functions over event lists for wasted-tokens + characteristic-event counts"
      contains: "def compute_wasted_tokens"
      min_lines: 100
  key_links:
    - from: "src/a2a_vs_mcp/race/metrics.py::compute_wasted_tokens"
      to: "src/a2a_vs_mcp/race/runners/* (Plan 09)"
      via: "runner calls at fault_observed time to populate wasted_tokens_before_detection field (D-40)"
      pattern: "compute_wasted_tokens"
    - from: "src/a2a_vs_mcp/race/metrics.py::aggregate_for_classifier"
      to: "src/a2a_vs_mcp/race/harness.py (Plan 10)"
      via: "harness calls after race_done; result passed as agg to failure_mode_classifier"
      pattern: "aggregate_for_classifier"
---

<objective>
Wave 2 metrics. Land `race/metrics.py` — pure functions over recorded event lists that compute (1) wasted-tokens-before-detection per fault (D-40), (2) per-lane characteristic event counts (D-37), and (3) the aggregate dict consumed by `failure_mode_classifier` (Plan 04).

Purpose: D-40 mandates wasted-tokens server-side at fault_observed time (master design §Cost computation). D-37 mandates lane-specific characteristic counts derived at headline-render time, NOT stored. The metrics module is small (pure functions, no state) and is consumed by both runners (per-fault wasted-tokens) and the harness (per-lane aggregate).

Output: One new module `src/a2a_vs_mcp/race/metrics.py`. ~100 LOC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-race-backend-lanes-harness-recovery/07-CONTEXT.md
@.planning/phases/07-race-backend-lanes-harness-recovery/07-RESEARCH.md
@.planning/phases/07-race-backend-lanes-harness-recovery/07-PATTERNS.md
@src/a2a_vs_mcp/evidence.py
@src/a2a_vs_mcp/race/turn.py

<interfaces>
RESEARCH §6 lines 970-994 contains the locked algorithm and table for these counts. RESEARCH §"characteristic_event count derivation" table (lines 962-968) defines field sources verbatim:

| Lane | Metric | Source |
|------|--------|--------|
| pure_mcp | median_retries | tool_call events with tool_name == fault.target AND turn_index > fault_inject_turn |
| pure_mcp | characteristic_tool | failure_script[0].target (constant per task; harness passes in) |
| pure_a2a | median_delegations | agent_msg events with message_type == "task_submit" AND turn_index > fault_inject_turn |
| hybrid | median_switches | count of (tool_call -> agent_msg) and (agent_msg -> tool_call) transitions in event order, AFTER fault_inject_turn |
| any (fallback) | median_turns_after_fault | max(turn_index) - fault_inject_turn per run |

D-40 wasted-tokens rule (master design §Cost computation, locked):

> Sum tokens_in + tokens_out across all llm_call events where t_call_start_ms is in [t_inject_ms, t_observed_ms] for the same lane.

NOTE: Phase 6 TRC-01 added tokens_in/tokens_out/t_call_start_ms to llm_call events. The runner is responsible for emitting llm_call events (RESEARCH Assumption A7). Phase 7 metrics consume them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create race/metrics.py with 6 pure functions over event lists</name>
  <read_first>
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-RESEARCH.md §6 lines 958-994 (full code shape)
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-PATTERNS.md metrics section (lines 297-326)
    - src/a2a_vs_mcp/evidence.py (module-level helpers idiom)
  </read_first>
  <action>
Create `src/a2a_vs_mcp/race/metrics.py`:

```
"""Race metrics — pure functions over event lists (D-37, D-40).

compute_wasted_tokens: sum tokens_in+tokens_out across llm_call events whose
t_call_start_ms falls in [t_inject_ms, t_observed_ms] for the same lane.
Master design §Cost computation, locked.

median_retries / median_delegations / median_switches / median_turns_after_fault:
per-lane characteristic-event counts derived at headline-render time per D-37.
Counts are NEVER stored; they're recomputed from the trace as needed.

aggregate_for_classifier: harness-side aggregator that produces the `agg` dict
consumed by failure_mode_classifier in classifier.py.
"""
from __future__ import annotations

import statistics
from typing import Any


def _find_fault_injected(events: list[dict], fault_id: str) -> dict | None:
    return next(
        (e for e in events
         if e.get("event_type") == "fault_injected" and e.get("fault_id") == fault_id),
        None,
    )


def _find_fault_observed(events: list[dict], fault_id: str) -> dict | None:
    return next(
        (e for e in events
         if e.get("event_type") == "fault_observed" and e.get("fault_id") == fault_id),
        None,
    )


def compute_wasted_tokens(events: list[dict], fault_id: str, lane: str) -> int:
    """D-40: sum tokens_in+tokens_out for llm_call events between inject and observe.

    Returns 0 if either fault_injected or fault_observed is missing
    (e.g., kept_going_without_noticing — no observation, no waste attributable).
    """
    fi = _find_fault_injected(events, fault_id)
    fo = _find_fault_observed(events, fault_id)
    if fi is None or fo is None:
        return 0
    t0 = fi.get("t_inject_ms", 0)
    t1 = fo.get("t_observed_ms", 0)
    total = 0
    for e in events:
        if e.get("event_type") != "llm_call":
            continue
        if e.get("lane") != lane:
            continue
        ts = e.get("t_call_start_ms", -1)
        if t0 <= ts <= t1:
            total += int(e.get("tokens_in", 0)) + int(e.get("tokens_out", 0))
    return total


def median_retries(events: list[dict], fault_id: str, target: str) -> int:
    """Per-run retry count = tool_call events with tool_name==target after fault_inject_turn."""
    fi = _find_fault_injected(events, fault_id)
    if fi is None:
        return 0
    inject_turn = fi.get("turn_index", -1)
    return sum(
        1 for e in events
        if e.get("event_type") == "tool_call"
        and e.get("tool_name") == target
        and e.get("turn_index", -1) > inject_turn
    )


def median_delegations(events: list[dict], fault_id: str) -> int:
    """Per-run delegation count = agent_msg events with message_type=='task_submit' after fault_inject_turn."""
    fi = _find_fault_injected(events, fault_id)
    if fi is None:
        return 0
    inject_turn = fi.get("turn_index", -1)
    return sum(
        1 for e in events
        if e.get("event_type") == "agent_msg"
        and e.get("message_type") == "task_submit"
        and e.get("turn_index", -1) > inject_turn
    )


def median_switches(events: list[dict], fault_id: str) -> int:
    """Per-run protocol-boundary crossings AFTER fault_inject_turn.

    A 'switch' = consecutive (tool_call -> agent_msg) or (agent_msg -> tool_call)
    transitions in event order. Counts pairs of adjacent qualifying events.
    """
    fi = _find_fault_injected(events, fault_id)
    if fi is None:
        return 0
    inject_turn = fi.get("turn_index", -1)
    qual = [
        e.get("event_type") for e in events
        if e.get("event_type") in ("tool_call", "agent_msg")
        and e.get("turn_index", -1) > inject_turn
    ]
    switches = 0
    for prev, cur in zip(qual, qual[1:]):
        if prev != cur:
            switches += 1
    return switches


def median_turns_after_fault(events: list[dict], fault_id: str) -> int:
    """Fallback metric per D-37: max(turn_index) - fault_inject_turn."""
    fi = _find_fault_injected(events, fault_id)
    if fi is None:
        return 0
    inject_turn = fi.get("turn_index", -1)
    max_turn = max((e.get("turn_index", -1) for e in events), default=inject_turn)
    return max(0, max_turn - inject_turn)


def aggregate_for_classifier(
    per_run_traces: list[list[dict]],
    task_id: str,
    lane: str,
    *,
    characteristic_tool: str | None = None,
) -> dict[str, Any]:
    """Aggregate across n runs into the `agg` dict consumed by failure_mode_classifier.

    Caller passes `characteristic_tool` from the task_config's failure_script[0].target
    (used only for pure_mcp's headline phrase per D-37).
    """
    n = len(per_run_traces)
    recovered_count = 0
    wasted = []
    ttff = []
    retries = []
    delegations = []
    switches = []
    turns = []
    for trace in per_run_traces:
        # Each trace owns at least one fault_id (failure_script may have multiples).
        fids = [e["fault_id"] for e in trace if e.get("event_type") == "fault_injected"]
        for fid in fids:
            wasted.append(compute_wasted_tokens(trace, fid, lane))
            fi = _find_fault_injected(trace, fid)
            fo = _find_fault_observed(trace, fid)
            if fi is not None and fo is not None:
                ttff.append(int(fo.get("t_observed_ms", 0)) - int(fi.get("t_inject_ms", 0)))
            if lane == "pure_mcp" and characteristic_tool:
                retries.append(median_retries(trace, fid, characteristic_tool))
            if lane == "pure_a2a":
                delegations.append(median_delegations(trace, fid))
            if lane == "hybrid":
                switches.append(median_switches(trace, fid))
            turns.append(median_turns_after_fault(trace, fid))
        # Recovery rate sourced from the run's done event.
        done = next((e for e in trace if e.get("event_type") == "done"), None)
        if done is not None and done.get("score_pass") is True:
            recovered_count += 1

    def _med(xs: list[int]) -> int:
        return int(statistics.median(xs)) if xs else 0

    agg: dict[str, Any] = {
        "recovery_rate": (recovered_count / n) if n else 0.0,
        "mean_wasted_tokens": (sum(wasted) / len(wasted)) if wasted else 0.0,
        "mean_ttff_ms": (sum(ttff) / len(ttff)) if ttff else 0.0,
        "median_turns_after_fault": _med(turns),
    }
    if lane == "pure_mcp":
        agg["median_retries"] = _med(retries)
        if characteristic_tool:
            agg["characteristic_tool"] = characteristic_tool
    elif lane == "pure_a2a":
        agg["median_delegations"] = _med(delegations)
    elif lane == "hybrid":
        agg["median_switches"] = _med(switches)
    return agg
```

CRITICAL constraints:
- All functions are module-level (no class) — match `evidence.py` idiom.
- Pure functions: no I/O, no time, no random, no state.
- Counts derived at call time per D-37 (NEVER stored on event payloads except for pre-existing `wasted_tokens_before_detection` on `fault_observed` which the runner populates by calling `compute_wasted_tokens`).
- Do NOT import classifier.py here (one-way dependency: classifier and runners both import metrics, never the other way).
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && python3 -c "
from a2a_vs_mcp.race.metrics import (
    compute_wasted_tokens, median_retries, median_delegations,
    median_switches, median_turns_after_fault, aggregate_for_classifier,
)

# Construct a tiny synthetic trace with one fault, observation, and 3 llm_calls.
events = [
    {'event_type': 'fault_injected', 'fault_id': 'f1', 'lane': 'pure_mcp', 't_inject_ms': 100, 'turn_index': 1},
    {'event_type': 'llm_call', 'lane': 'pure_mcp', 't_call_start_ms': 50, 'tokens_in': 10, 'tokens_out': 5},   # before inject — excluded
    {'event_type': 'llm_call', 'lane': 'pure_mcp', 't_call_start_ms': 120, 'tokens_in': 200, 'tokens_out': 50},
    {'event_type': 'tool_call', 'tool_name': 'github_api.get_repo_metadata', 'turn_index': 2},
    {'event_type': 'tool_call', 'tool_name': 'github_api.get_repo_metadata', 'turn_index': 3},
    {'event_type': 'llm_call', 'lane': 'pure_mcp', 't_call_start_ms': 180, 'tokens_in': 80, 'tokens_out': 20},
    {'event_type': 'fault_observed', 'fault_id': 'f1', 't_observed_ms': 200, 'turn_index': 4},
    {'event_type': 'llm_call', 'lane': 'pure_mcp', 't_call_start_ms': 250, 'tokens_in': 999, 'tokens_out': 1},  # after observe — excluded
    {'event_type': 'agent_msg', 'message_type': 'task_submit', 'turn_index': 3},
    {'event_type': 'done', 'score_pass': True},
]
assert compute_wasted_tokens(events, 'f1', 'pure_mcp') == 200+50+80+20  # 350
assert median_retries(events, 'f1', 'github_api.get_repo_metadata') == 2
assert median_delegations(events, 'f1') == 1
assert median_turns_after_fault(events, 'f1') == 3  # max=4, inject=1
agg = aggregate_for_classifier([events], 'summarize_repo', 'pure_mcp', characteristic_tool='github_api.get_repo_metadata')
assert agg['recovery_rate'] == 1.0
assert agg['mean_wasted_tokens'] == 350
assert agg['median_retries'] == 2
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^from __future__ import annotations" src/a2a_vs_mcp/race/metrics.py` returns line 1.
    - `grep -cE "^def (compute_wasted_tokens|median_retries|median_delegations|median_switches|median_turns_after_fault|aggregate_for_classifier)" src/a2a_vs_mcp/race/metrics.py` outputs `6`.
    - `grep -c "^class " src/a2a_vs_mcp/race/metrics.py` outputs `0` (no classes).
    - `grep -c "import time\|datetime.now\|random" src/a2a_vs_mcp/race/metrics.py` outputs `0` (pure functions, no time/random).
    - Verify automated block exits 0 — exercises wasted-tokens math, retry counting, delegation counting, fallback turn count, aggregate shape.
    - `wc -l src/a2a_vs_mcp/race/metrics.py | awk '{print $1}'` ≥ 90.
    - `pytest tests/race/ -x -q` (Phase 6 + Plans 02, 04 regression) exits 0.
  </acceptance_criteria>
  <done>metrics.py implements 6 pure functions; smoke test passes (wasted_tokens=350, median_retries=2, recovery_rate=1.0).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trace event ↔ metrics | Untrusted (replay path may load adversarial fixture) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | malformed event missing required fields | mitigate | All accessors use `.get(..., default)` so missing fields produce 0 / -1 sentinel, not crashes |
| T-07-05-02 | Information Disclosure | wasted-tokens computation reveals run shape | accept | Counts are publicly intended (master design §Cost computation says display in headlines) |
| T-07-05-03 | Tampering | event ordering manipulation in trace | accept | Replay path reads append-only ndjson; Phase 6 RunWriter (single-writer arbiter D-05) prevents interleaving |
</threat_model>

<verification>
- `pytest tests/race/ -x -q` exits 0 (Phase 6 + Plans 02, 04 regression).
- All 6 grep gates pass.
- Smoke-test verify command exits 0.
- Plan 11 `test_metrics_wasted_tokens.py` later asserts the [t_inject_ms, t_observed_ms] window inclusive bounds and lane filtering.
</verification>

<success_criteria>
- D-40 wasted-tokens semantics shipped (exclusive lane filter, inclusive time window).
- D-37 characteristic-event counts shipped per lane with fallback.
- aggregate_for_classifier provides the agg-shape consumed by Plan 04's failure_mode_classifier.
</success_criteria>

<output>
After completion, create `.planning/phases/07-race-backend-lanes-harness-recovery/07-05-SUMMARY.md`.
</output>
