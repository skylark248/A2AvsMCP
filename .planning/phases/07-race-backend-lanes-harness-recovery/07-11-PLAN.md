---
phase: 07-race-backend-lanes-harness-recovery
plan: 11
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10]
files_modified:
  - tests/race/test_runner_pure_mcp.py
  - tests/race/test_runner_pure_a2a.py
  - tests/race/test_runner_hybrid.py
  - tests/race/test_harness.py
  - tests/race/test_classifier_detector.py
  - tests/race/test_classifier_regex.py
  - tests/race/test_failure_mode_classifier.py
  - tests/race/test_metrics.py
  - tests/race/test_haiku_judge.py
  - tests/race/test_task_registries.py
  - tests/race/test_hardness_coverage.py
  - tests/race/test_mocks_chokepoint.py
  - tests/race/fixtures/traces/
  - tests/race/fixtures/regex_corpus.json
autonomous: true
requirements: [RACE-01, RACE-02, RACE-03, RACE-04, RACE-05, RACE-06, RACE-07]
tags: [tests, fixtures, validation, nyquist]

must_haves:
  truths:
    - "All 12 test files use unittest.TestCase shape mirroring tests/race/test_inject_fault.py (project convention)"
    - "tests/race/fixtures/traces/ contains 9 fictional fault traces (per master design §The Assignment) covering recovered, gave_up, kept_going_without_noticing, kept_going_to_failure, indeterminate per task"
    - "tests/race/fixtures/regex_corpus.json contains exactly 50 samples (25 acks, 25 non-acks) for D-36 false-positive target"
    - "test_classifier_detector.py: K=3 state machine over 9 fictional traces; asserts terminal tag matches expected per fixture (RACE-04)"
    - "test_classifier_regex.py: is_acknowledging_fault over 50-sample corpus; FP rate < 10% per D-36"
    - "test_failure_mode_classifier.py: exercises all 6 templates (recovered, gave_up, kept_going_without_noticing, kept_going_to_failure, indeterminate, lane_failed) per lane (RACE-06)"
    - "test_harness.py: mocks _RUNNERS; asserts (a) Semaphore(8) caps concurrent in-flight count; (b) InjectedFaultError NOT caught by retry; (c) RateLimitError IS retried up to 3 attempts; (d) per-run timeout produces lane_failed/timeout ScoreCard (RACE-03)"
    - "test_runner_pure_mcp.py: in-process MCPClient against mcp_servers/race_github; end-to-end summarize_repo with empty failure_script returns RaceResult(lane='pure_mcp') (RACE-02)"
    - "test_runner_pure_a2a.py: registers FixtureBackedAgentHandler on fresh A2ABroker; broker.send_task (NOT send_message); asserts trace records agent_msg event (RACE-02)"
    - "test_runner_hybrid.py: exercises all 4 on_fault enum values (retry_once, delegate, abort, continue) with synthetic hybrid_plan (RACE-02)"
    - "test_metrics.py: asserts compute_wasted_tokens window math + median_retries / median_delegations / median_switches per fixture trace (RACE-04)"
    - "test_haiku_judge.py: mocks anthropic.AsyncAnthropic.messages.create; asserts cache_control={'type':'ephemeral'} on system prompt; temperature=0; missing ANTHROPIC_API_KEY -> RuntimeError; key value never appears in logs (RACE-05)"
    - "test_task_registries.py: each task __init__ exports TARGETS dict + BINDS dict; Pydantic validator rejects unknown target ID and unknown bind key at first import (RACE-01, RACE-05)"
    - "test_hardness_coverage.py: asserts D-30 matrix exactly — LONG_CHAIN ⊆ {summarize_repo, book_travel}; RATE_PRESSURE ⊆ {summarize_repo, book_travel}; SCHEMA_VARIANCE ⊆ {summarize_repo, negotiate_meeting}; MULTI_SOURCE_SYNTHESIS ⊆ {negotiate_meeting, book_travel} (RACE-01)"
    - "test_mocks_chokepoint.py: greps source — every public callable in race/mocks/{github,calendar,travel}.py contains inject_fault(; every @mcp.tool() in mcp_servers/race_*.py routes through race.mocks.* (extends Phase 6 D-13 IRON RULE) (RACE-07, D-25)"
    - "All 37 existing race tests still green: pytest tests/race/ -x exits 0"
    - "Total test count after Phase 7 ≥ 49 (37 existing + 12 new)"
  artifacts:
    - path: "tests/race/fixtures/traces/"
      provides: "9 fictional fault traces (json) for Detector + replay-symmetry tests"
      min_files: 9
    - path: "tests/race/fixtures/regex_corpus.json"
      provides: "50-sample agent_msg corpus for regex FP test"
      contains: "samples"
  key_links:
    - from: "test_classifier_detector.py + test_metrics.py"
      to: "tests/race/fixtures/traces/*.json"
      via: "load fixture, replay events through Detector / metrics; assert known terminal tag / count per fixture"
      pattern: "fixtures/traces"
    - from: "test_mocks_chokepoint.py"
      to: "src/a2a_vs_mcp/race/mocks/*.py + src/a2a_vs_mcp/mcp_servers/race_*.py"
      via: "grep `inject_fault(` per public callable; grep mock module imports per @mcp.tool()"
      pattern: "inject_fault"
    - from: "test_harness.py"
      to: "race/harness.py"
      via: "monkeypatch _RUNNERS to fake coroutines that raise InjectedFaultError / RateLimitError / TimeoutError; assert retry classifier behavior"
      pattern: "_RUNNERS"
---

<objective>
Wave 6 test suite. Land 12 test files + fixture authoring (9 trace fixtures + 50-sample regex corpus) that close every RACE-01..07 success criterion via grep-verifiable assertions, plus the IRON RULE D-25 source grep gate, the D-30 hardness coverage matrix, and the harness retry-classifier contract that protects D-38.

Purpose: gate Phase 7 verification. Without these tests Phase 7 has no objective truth-bearer for "the lanes work, the harness obeys D-38, the recovery state machine emits the locked tags."

Output: 12 test files + 10 fixture files. Total ~1500 LOC tests; ~600 LOC fixture data.

Coverage table (RaceReq → test):
| Req | Test file | What it asserts |
|-----|-----------|-----------------|
| RACE-01 | test_task_registries.py + test_hardness_coverage.py | HardnessProfile dataclass + D-30 matrix |
| RACE-02 | test_runner_pure_mcp.py + test_runner_pure_a2a.py + test_runner_hybrid.py | Each lane returns RaceResult |
| RACE-03 | test_harness.py | Semaphore + transient-only retry + per-run timeout |
| RACE-04 | test_classifier_detector.py + test_classifier_regex.py + test_metrics.py | K=3 + regex + counts |
| RACE-05 | test_haiku_judge.py + test_task_registries.py | Per-task scorers + registries |
| RACE-06 | test_failure_mode_classifier.py | 6-template headlines per lane |
| RACE-07 | test_mocks_chokepoint.py | IRON RULE D-25 grep gate |
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-race-backend-lanes-harness-recovery/07-CONTEXT.md
@.planning/phases/07-race-backend-lanes-harness-recovery/07-RESEARCH.md
@.planning/phases/07-race-backend-lanes-harness-recovery/07-PATTERNS.md
@tests/race/test_inject_fault.py
@src/a2a_vs_mcp/race/classifier.py
@src/a2a_vs_mcp/race/metrics.py
@src/a2a_vs_mcp/race/harness.py
@src/a2a_vs_mcp/race/runners/__init__.py
@src/a2a_vs_mcp/race/judges/haiku.py
@src/a2a_vs_mcp/race/tasks/__init__.py
@src/a2a_vs_mcp/race/mocks/__init__.py

<interfaces>
RESEARCH §8 lines 1180-1260 lays out the 12-file test layout and per-test method names. All tests use unittest.TestCase mirroring tests/race/test_inject_fault.py. Local helpers `_make_recorder()` and `_cleanup(run_id)` reused across files.

Fixture trace shape (tests/race/fixtures/traces/<name>.json):
```
{
  "name": "summarize_repo_recovered_quickly",
  "lane": "pure_mcp",
  "task_id": "summarize_repo",
  "expected_terminal_tag": "recovered",
  "events": [
    {"event_type": "fault_injected", "fault_id": "f1", "fault_kind": "rate_limit_429", "target": "github_api.list_files", "turn_index": 2, "t_inject_ms": 100},
    {"event_type": "tool_call", "tool_name": "list_files", "status": "error", "error_kind": "rate_limit_429", "turn_index": 2},
    {"event_type": "agent_msg", "text": "I hit a rate limit on list_files. Let me back off and retry in a moment.", "turn_index": 3},
    {"event_type": "tool_call", "tool_name": "list_files", "status": "ok", "turn_index": 4},
    {"event_type": "done", "score_pass": true}
  ]
}
```

9 fixtures cover (3 tasks × 3 outcomes — recovered, gave_up, kept_going_without_noticing) baseline; 4 of them additionally serve as kept_going_to_failure / indeterminate proxies via score_pass + race_done variants.

Regex corpus (tests/race/fixtures/regex_corpus.json):
```
{
  "samples": [
    {"text": "I hit a rate limit. Retrying.", "is_ack": true},
    ... 24 more acks ...
    {"text": "Looking up the next file now.", "is_ack": false},
    ... 24 more non-acks ...
  ]
}
```
50 total. FP rate target < 10% on the non-ack 25.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author 9 fictional fault trace fixtures under tests/race/fixtures/traces/</name>
  <read_first>
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-RESEARCH.md §8 lines 1180-1240 (fixture layout)
    - master design §The Assignment (canonical fault trace examples)
  </read_first>
  <action>
Create the directory and 9 JSON files (one per scenario):

```
tests/race/fixtures/traces/
  summarize_repo_recovered.json          (expected_terminal_tag=recovered)
  summarize_repo_gave_up.json            (expected_terminal_tag=gave_up)
  summarize_repo_kept_going_silent.json  (expected_terminal_tag=kept_going_without_noticing)
  negotiate_meeting_recovered.json       (expected_terminal_tag=recovered)
  negotiate_meeting_kept_going.json      (expected_terminal_tag=kept_going_to_failure)
  negotiate_meeting_indeterminate.json   (expected_terminal_tag=indeterminate)  # race_done with no done
  book_travel_recovered.json             (expected_terminal_tag=recovered)
  book_travel_gave_up.json               (expected_terminal_tag=gave_up)
  book_travel_kept_going_to_failure.json (expected_terminal_tag=kept_going_to_failure)
```

Each JSON file follows the shape under `<interfaces>`. Each fault has K=3 worth of events between `fault_injected` and either `agent_msg_acknowledging_fault` (→ recovered/gave_up branch) or none (→ kept_going_*). The `indeterminate` fixture omits the `done` event entirely; an explicit final event `{"event_type": "race_done"}` triggers `Detector.finalize_at_race_done_no_done()`.

Hand-author the 9 files using master design §The Assignment as ground truth. Each fixture's `expected_terminal_tag` is asserted in test_classifier_detector.py.
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && python3 -c "
import json, os, glob
files = sorted(glob.glob('tests/race/fixtures/traces/*.json'))
assert len(files) == 9, f'expected 9 fixtures, found {len(files)}'
for f in files:
    d = json.load(open(f))
    assert 'name' in d and 'lane' in d and 'task_id' in d and 'expected_terminal_tag' in d and 'events' in d, f'{f} missing keys'
    assert d['expected_terminal_tag'] in {'recovered','gave_up','kept_going_without_noticing','kept_going_to_failure','indeterminate'}, f'{f} bad tag'
    assert any(e.get('event_type') == 'fault_injected' for e in d['events']), f'{f} no fault_injected'
print('OK', len(files))
"</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/race/fixtures/traces/*.json | wc -l` outputs `9`.
    - Each fixture has `name`, `lane`, `task_id`, `expected_terminal_tag`, `events` keys.
    - Each `expected_terminal_tag` is one of the 5 locked recovery tag values.
    - Each fixture has at least one `fault_injected` event.
    - Verify-automated block exits 0.
  </acceptance_criteria>
  <done>9 trace fixtures committed; ground truth for Detector + replay-symmetry tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author 50-sample regex corpus at tests/race/fixtures/regex_corpus.json</name>
  <read_first>
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-RESEARCH.md §6 lines 870-905 (regex + negation guard)
    - master design §Recovery detection (regex source)
  </read_first>
  <action>
Create `tests/race/fixtures/regex_corpus.json`:

```
{
  "version": "v1-2026-04-28",
  "fp_target": 0.10,
  "samples": [
    {"text": "I hit a rate limit. Retrying.", "is_ack": true},
    {"text": "The list_files call returned a 429. Let me back off.", "is_ack": true},
    {"text": "I see an error from get_repo_metadata. Retrying with backoff.", "is_ack": true},
    {"text": "API gave a 500. I'll try again in a moment.", "is_ack": true},
    {"text": "Schema mismatch on propose_time. Switching to a different format.", "is_ack": true},
    {"text": "search_flights returned a partial response. Detected an error and recovering.", "is_ack": true},
    {"text": "I noticed the rate limit. Trying alternate path.", "is_ack": true},
    {"text": "Booking endpoint failed with 503. Retrying.", "is_ack": true},
    {"text": "Caught a timeout from list_files. Retrying.", "is_ack": true},
    {"text": "I detected an injected fault. Falling back to cached data.", "is_ack": true},
    {"text": "Rate limited on get_free_busy. Sleeping.", "is_ack": true},
    {"text": "I see the error response. Adjusting and retrying.", "is_ack": true},
    {"text": "Got an HTTP 429 — known transient — retrying.", "is_ack": true},
    {"text": "search_hotels failed; the response shape was unexpected; retrying.", "is_ack": true},
    {"text": "Tool call returned status error. Recovering with retry.", "is_ack": true},
    {"text": "Rate limit hit. Will retry shortly.", "is_ack": true},
    {"text": "I encountered a fault on read_file. Trying again.", "is_ack": true},
    {"text": "Retrying after the previous error response.", "is_ack": true},
    {"text": "Detected partial commit failure. Reverting and retrying.", "is_ack": true},
    {"text": "API timeout. Backing off and retrying.", "is_ack": true},
    {"text": "I observe an error from book_itinerary. Retrying.", "is_ack": true},
    {"text": "Got 5xx. Recovering.", "is_ack": true},
    {"text": "Schema variance in list_files response. Adjusting parsing.", "is_ack": true},
    {"text": "list_files threw an error — let me retry once.", "is_ack": true},
    {"text": "I caught an exception. Retrying with backoff.", "is_ack": true},
    {"text": "Looking up the next file now.", "is_ack": false},
    {"text": "Continuing to read repository metadata.", "is_ack": false},
    {"text": "I am about to call search_flights for the return leg.", "is_ack": false},
    {"text": "Free-busy windows look good. Proposing a time.", "is_ack": false},
    {"text": "Selected the cheapest combination for the trip.", "is_ack": false},
    {"text": "Reading the entry point file now.", "is_ack": false},
    {"text": "I will call propose_time with these owners.", "is_ack": false},
    {"text": "Booking the itinerary with flight F-002 and F-102.", "is_ack": false},
    {"text": "Repository purpose: API gateway. Modules: routing, auth, logging.", "is_ack": false},
    {"text": "Three calendars fetched successfully.", "is_ack": false},
    {"text": "I do not retry — moving on to the next step.", "is_ack": false},
    {"text": "I am not retrying because everything succeeded.", "is_ack": false},
    {"text": "No errors so far. Continuing.", "is_ack": false},
    {"text": "I have all the data I need. Drafting summary.", "is_ack": false},
    {"text": "Found 3 candidate hotels in NYC.", "is_ack": false},
    {"text": "Confirming flight selection with the user.", "is_ack": false},
    {"text": "Switching to the alternate proposal because two owners conflict.", "is_ack": false},
    {"text": "Initial repo metadata loaded.", "is_ack": false},
    {"text": "Booked itinerary id BK-441 successfully.", "is_ack": false},
    {"text": "Three meeting candidates found across all attendees.", "is_ack": false},
    {"text": "Total cost $1280 — within budget.", "is_ack": false},
    {"text": "Proceeding to the next phase of the task.", "is_ack": false},
    {"text": "I should ask the user which destination to book first.", "is_ack": false},
    {"text": "Listing the top three modules of the repo.", "is_ack": false},
    {"text": "Final answer drafted.", "is_ack": false}
  ]
}
```

Total exactly 50 — 25 acks (indices 0-24) + 25 non-acks (indices 25-49). Every ack mentions an error/retry pattern; every non-ack avoids retry/error verbs. The "I do not retry" + "I am not retrying because everything succeeded" entries are the negation-guard test cases (D-36) — without negation guard regex would falsely match them.
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && python3 -c "
import json
d = json.load(open('tests/race/fixtures/regex_corpus.json'))
assert d['fp_target'] == 0.10
assert len(d['samples']) == 50
acks = [s for s in d['samples'] if s['is_ack']]
nonacks = [s for s in d['samples'] if not s['is_ack']]
assert len(acks) == 25 and len(nonacks) == 25
print('OK', len(d['samples']))
"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; d=json.load(open('tests/race/fixtures/regex_corpus.json')); assert len(d['samples'])==50"` exits 0.
    - Verify-automated block confirms 25 acks + 25 non-acks.
    - At least 2 negation-guard test cases present ("I do not retry", "I am not retrying because everything succeeded").
  </acceptance_criteria>
  <done>50-sample regex corpus committed with negation-guard cases.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write classifier-side tests (test_classifier_detector.py + test_classifier_regex.py + test_failure_mode_classifier.py + test_metrics.py)</name>
  <read_first>
    - tests/race/test_inject_fault.py (TestCase shape, _make_recorder helper)
    - src/a2a_vs_mcp/race/classifier.py
    - src/a2a_vs_mcp/race/metrics.py
    - tests/race/fixtures/traces/ (Task 1 output)
    - tests/race/fixtures/regex_corpus.json (Task 2 output)
  </read_first>
  <action>
Create 4 files. Each file mirrors `tests/race/test_inject_fault.py` shape (unittest.TestCase, module-level helpers).

**A. tests/race/test_classifier_detector.py**:
- `class TestDetectorOnFixtures(unittest.TestCase)` with `test_terminal_tag_matches_fixture_<name>` — one method per fixture in tests/race/fixtures/traces/. Each: load json, replay events through Detector, assert `Detector.finalize_at_done(score_pass)` (or `finalize_at_race_done_no_done()` for indeterminate fixture) returns the fixture's `expected_terminal_tag`.
- `class TestDetectorK3Window(unittest.TestCase)` with `test_consume_no_observation_within_k3_returns_kept_going_silent` — synthesizes a fault_injected event followed by 4 turn-defining events with no acknowledging agent_msg; asserts terminal tag = `kept_going_without_noticing`.
- `class TestDetectorReplaySymmetry(unittest.TestCase)` with `test_replay_yields_same_tag_as_live` — runs the same fixture through Detector twice (once "live" feeding events one-by-one, once "replay" via list iteration); asserts identical tag.

**B. tests/race/test_classifier_regex.py**:
- `class TestAckRegexCorpus(unittest.TestCase)` with `test_corpus_fp_rate_below_10pct` — loads regex_corpus.json; runs `is_acknowledging_fault` over each sample; computes FP = (false-positive count on non-acks) / 25; asserts `fp_rate < 0.10`.
- `class TestNegationGuard(unittest.TestCase)` with `test_negation_guard_blocks_i_do_not_retry` and `test_negation_guard_blocks_i_am_not_retrying` — explicit assertions on the two negation-cases.

**C. tests/race/test_failure_mode_classifier.py**:
- `class TestSixTemplatesPureMcp(unittest.TestCase)` with `test_recovered_template`, `test_gave_up_template`, `test_kept_going_without_noticing_template`, `test_kept_going_to_failure_template`, `test_indeterminate_template`, `test_lane_failed_template` — synthesizes minimal `per_run_tags` + `agg` for each branch; asserts headline string equals the locked template (master design §failure_mode_classifier verbatim).
- `class TestSixTemplatesPureA2a` + `class TestSixTemplatesHybrid` — same 6 methods each, asserting characteristic_event phrase per D-37 (`retried {tool} {n} times` for pure_mcp; `delegated {n} times` for pure_a2a; `switched protocol path {n} times` for hybrid).

**D. tests/race/test_metrics.py**:
- `class TestComputeWastedTokens(unittest.TestCase)` with `test_window_math_sums_only_within_inject_to_observed`. Uses a hand-built event list with 3 llm_call events, only 2 of which fall in [t_inject_ms, t_observed_ms]; asserts `compute_wasted_tokens` returns the sum of those 2 only.
- `class TestMedianRetries(unittest.TestCase)` + `TestMedianDelegations` + `TestMedianSwitches` + `TestMedianTurnsAfterFault` — one method each over fixture traces; asserts known median per fixture.
- `class TestAggregateForClassifier(unittest.TestCase)` with `test_aggregate_returns_required_keys` — verifies returned dict has `recovery_rate`, `mean_wasted_tokens`, `mean_ttff_ms`, `characteristic_tool`, `median_retries`, `median_delegations`, `median_switches`, `median_turns_after_fault`, `lane_failed_reason` keys.
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && pytest tests/race/test_classifier_detector.py tests/race/test_classifier_regex.py tests/race/test_failure_mode_classifier.py tests/race/test_metrics.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/race/test_classifier_detector.py -x -q` exits 0.
    - `pytest tests/race/test_classifier_regex.py -x -q` exits 0.
    - `pytest tests/race/test_failure_mode_classifier.py -x -q` exits 0.
    - `pytest tests/race/test_metrics.py -x -q` exits 0.
    - `grep -c "fp_rate" tests/race/test_classifier_regex.py` outputs at least `1`.
    - `grep -c "test_replay_yields_same_tag_as_live" tests/race/test_classifier_detector.py` outputs `1`.
    - All 6 templates exercised per lane (`grep -c "template" tests/race/test_failure_mode_classifier.py` ≥ 18).
  </acceptance_criteria>
  <done>Classifier + metrics tests cover RACE-04 + RACE-06 + replay-symmetry guarantee.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write runner tests (test_runner_pure_mcp.py + test_runner_pure_a2a.py + test_runner_hybrid.py)</name>
  <read_first>
    - src/a2a_vs_mcp/race/runners/pure_mcp.py + pure_a2a.py + hybrid.py (Plan 09)
    - tests/race/test_inject_fault.py
  </read_first>
  <action>
**A. tests/race/test_runner_pure_mcp.py**:
- `class TestPureMcpEndToEnd(unittest.TestCase)` with:
  - `test_summarize_repo_no_faults_returns_score_pass` — empty failure_script; assert `RaceResult.score_card.success` true.
  - `test_negotiate_meeting_no_faults` — same.
  - `test_book_travel_no_faults` — same.
  - `test_inject_fault_records_fault_injected_and_observed` — failure_script with one rate_limit_429 fault on github_api.list_files; assert recorder events contain both `fault_injected` and `fault_observed` event types.
  - `test_run_id_threaded_into_recorder` — assert `recorder.run_id == run_id`.

**B. tests/race/test_runner_pure_a2a.py**:
- `class TestPureA2AEndToEnd(unittest.TestCase)` with:
  - `test_negotiate_meeting_via_broker_send_task` — assert recorder events include at least one `agent_msg` event.
  - `test_send_message_method_does_not_appear_in_runner` — `grep_count` of `send_message` in `src/a2a_vs_mcp/race/runners/pure_a2a.py` is 0 (negative grep gate against CONTEXT.md D-24 typo).
  - `test_summarize_repo_via_a2a_returns_score_card`.
  - `test_book_travel_via_a2a_returns_score_card`.

**C. tests/race/test_runner_hybrid.py**:
- `class TestHybridOnFault(unittest.TestCase)` with:
  - `test_on_fault_retry_once_succeeds_on_second_attempt` — synthetic hybrid_plan with `on_fault: retry_once`; first attempt raises InjectedFaultError, second succeeds; assert step result captured.
  - `test_on_fault_retry_once_fails_on_second_attempt_raises` — both attempts raise; assert hybrid runner records the failure.
  - `test_on_fault_delegate_uses_alt_step` — assert alt step is dispatched after fault.
  - `test_on_fault_abort_propagates_failure` — assert RaceResult.score_card.success is false.
  - `test_on_fault_continue_skips_step_returns_none` — assert next step still runs.
  - `test_hybrid_uses_pre_scripted_plan_only` — confirms hybrid runner does NOT call any LLM (`grep -c "messages.create" src/a2a_vs_mcp/race/runners/hybrid.py` outputs 0; D-21 IRON RULE for v1).
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && pytest tests/race/test_runner_pure_mcp.py tests/race/test_runner_pure_a2a.py tests/race/test_runner_hybrid.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/race/test_runner_pure_mcp.py tests/race/test_runner_pure_a2a.py tests/race/test_runner_hybrid.py -x -q` exits 0.
    - `grep -c "send_message" src/a2a_vs_mcp/race/runners/pure_a2a.py` outputs `0` (typo enforcement).
    - `grep -c "send_task" src/a2a_vs_mcp/race/runners/pure_a2a.py` outputs at least `1`.
    - `grep -c "messages.create" src/a2a_vs_mcp/race/runners/hybrid.py` outputs `0` (D-21 — no LLM in hybrid v1).
    - All 4 on_fault enum values exercised in test_runner_hybrid.py (`grep -cE "retry_once|delegate|abort|continue" tests/race/test_runner_hybrid.py` ≥ 4).
  </acceptance_criteria>
  <done>RACE-02 covered for all 3 lanes; D-21 + D-24 enforced via grep gates.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Write test_harness.py asserting D-38 IRON RULE + Semaphore + per-run timeout</name>
  <read_first>
    - src/a2a_vs_mcp/race/harness.py (Plan 10)
    - src/a2a_vs_mcp/race/failure.py (InjectedFaultError)
  </read_first>
  <action>
Create `tests/race/test_harness.py`:
- `class TestHarnessSemaphoreCap(unittest.TestCase)` with `test_concurrent_in_flight_capped_at_eight` — monkeypatch `_RUNNERS["pure_mcp"]` with a fake coroutine that records start/end timestamps; submit 16 runs at n=16; assert max overlapping in-flight count ≤ 8 (sliding-window over recorded timestamps).
- `class TestHarnessRetryClassifier(unittest.TestCase)` with:
  - `test_injected_fault_not_retried` — runner raises InjectedFaultError on first attempt; assert runner is called exactly once (no retry).
  - `test_rate_limit_error_retried_up_to_3` — runner raises `anthropic.RateLimitError` twice then succeeds; assert exactly 3 invocations.
  - `test_connection_error_retried` — runner raises `anthropic.APIConnectionError` once then succeeds; assert exactly 2 invocations.
  - `test_unknown_exception_propagates` — runner raises `ValueError`; assert ValueError propagates without retry.
- `class TestHarnessTimeout(unittest.TestCase)` with `test_per_run_timeout_produces_lane_failed_timeout_score` — runner sleeps 121s (mock asyncio.sleep + monkeypatch PER_RUN_TIMEOUT_S to 0.1); assert returned `RaceResult.score_card.failure_mode == "lane_failed"` and `lane_failed_reason == "timeout"` reachable via aggregate.
- `class TestHarnessRaceDoneEmission(unittest.TestCase)` with `test_race_done_emitted_via_ws_emitter_at_end` — collect events from a list-backed ws_emitter; assert exactly one `race_done` event after all runs complete; assert `total_runs` field equals `len(lanes) * len(task_specs) * n`.
- `class TestHarnessHeadlineGenerated(unittest.TestCase)` with `test_headline_present_per_lane_task_pair` — run dry config; assert each (lane, task) key in returned dict has a non-empty `headline` string from one of the 6 templates.
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && pytest tests/race/test_harness.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/race/test_harness.py -x -q` exits 0.
    - `grep -c "InjectedFaultError" tests/race/test_harness.py` outputs at least `2`.
    - `grep -c "RateLimitError" tests/race/test_harness.py` outputs at least `1`.
    - `grep -c "Semaphore\|in_flight\|in-flight" tests/race/test_harness.py` outputs at least `1`.
    - `grep -c "race_done" tests/race/test_harness.py` outputs at least `1`.
  </acceptance_criteria>
  <done>RACE-03 + D-38 contract tests landed; injected faults provably never retried.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Write test_haiku_judge.py + test_task_registries.py + test_hardness_coverage.py</name>
  <read_first>
    - src/a2a_vs_mcp/race/judges/haiku.py (Plan 06)
    - src/a2a_vs_mcp/race/tasks/__init__.py (Plan 08)
    - src/a2a_vs_mcp/race/tasks/loader.py (Plan 08)
  </read_first>
  <action>
**A. tests/race/test_haiku_judge.py**:
- `class TestHaikuJudgeDeterminism(unittest.TestCase)` with:
  - `test_temperature_zero_passed_to_create` — monkeypatch `anthropic.AsyncAnthropic` mock client; capture `messages.create` call kwargs; assert `temperature == 0.0`.
  - `test_cache_control_ephemeral_on_system_prompt` — assert system message contains `cache_control={'type':'ephemeral'}`.
  - `test_haiku_model_id` — assert `model='claude-haiku-4-5'`.
  - `test_missing_api_key_raises_runtime_error` — unset env; instantiate; assert RuntimeError raised; assert key value not in exception args.
  - `test_judge_emits_llm_call_event_on_recorder` — pass mock recorder; assert `recorder.events` contains an `llm_call` event with `role='judge'` after `judge()` call.

**B. tests/race/test_task_registries.py**:
- `class TestTargetsRegistries(unittest.TestCase)` with:
  - `test_each_task_exports_targets_dict` — for each of 3 tasks, assert `from a2a_vs_mcp.race.tasks.<task_id> import TARGETS` works and `isinstance(TARGETS, dict)` and `len(TARGETS) >= 1`.
  - `test_each_task_exports_binds_dict` — same shape for BINDS.
  - `test_unknown_target_in_failure_script_raises_validation_error_at_import` — synthesize a temporary task_config.yaml with unknown target; reload; assert pydantic ValidationError.
  - `test_unknown_bind_in_hybrid_plan_step_raises_validation_error_at_import` — same for unknown bind.
- `class TestPerTaskScorers(unittest.TestCase)` with:
  - `test_summarize_repo_haiku_only` — `grep -c "HaikuJudge\|race.judges" src/a2a_vs_mcp/race/tasks/summarize_repo/__init__.py` ≥ 1.
  - `test_negotiate_meeting_no_haiku` — `grep -c "HaikuJudge\|race.judges" src/a2a_vs_mcp/race/tasks/negotiate_meeting/__init__.py` outputs `0` (D-43 enforcement).
  - `test_book_travel_composite_haiku_and_structural` — `grep -c "HaikuJudge" src/a2a_vs_mcp/race/tasks/book_travel/__init__.py` ≥ 1; structural budget check present.

**C. tests/race/test_hardness_coverage.py**:
- `class TestHardnessCoverageMatrix(unittest.TestCase)` with:
  - `test_long_chain_in_summarize_and_book_travel` — load TASK_CONFIGS; assert `LONG_CHAIN in summarize_repo.hardness_profile.types` and `LONG_CHAIN in book_travel.hardness_profile.types`.
  - `test_rate_pressure_in_summarize_and_book_travel`.
  - `test_schema_variance_in_summarize_and_negotiate_meeting`.
  - `test_multi_source_synthesis_in_negotiate_and_book_travel`.
  - `test_each_v1_hardness_type_covers_at_least_two_tasks` — for each `HardnessType`, count tasks containing it; assert count ≥ 2 (D-30).
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && pytest tests/race/test_haiku_judge.py tests/race/test_task_registries.py tests/race/test_hardness_coverage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/race/test_haiku_judge.py tests/race/test_task_registries.py tests/race/test_hardness_coverage.py -x -q` exits 0.
    - `grep -c "test_temperature_zero" tests/race/test_haiku_judge.py` outputs `1`.
    - `grep -c "test_each_v1_hardness_type_covers_at_least_two_tasks" tests/race/test_hardness_coverage.py` outputs `1`.
    - `grep -c "test_negotiate_meeting_no_haiku" tests/race/test_task_registries.py` outputs `1` (D-43 enforcement).
  </acceptance_criteria>
  <done>RACE-01 (D-30 matrix) + RACE-05 (per-task scorers) + Haiku judge determinism + D-43 negotiate_meeting structural-only — all covered.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Write test_mocks_chokepoint.py — IRON RULE D-25 grep gate</name>
  <read_first>
    - src/a2a_vs_mcp/race/mocks/github.py + calendar.py + travel.py
    - src/a2a_vs_mcp/mcp_servers/race_github.py + race_calendar.py + race_travel.py
    - tests/race/test_inject_fault.py (Phase 6 D-13 grep test for shape reference)
  </read_first>
  <action>
Create `tests/race/test_mocks_chokepoint.py`:

```
"""IRON RULE D-25: every public mock callable goes through inject_fault().

Extends Phase 6 D-13 grep gate from race/failure.py to race/mocks/* and
mcp_servers/race_*.py — single fault chokepoint enforced by source grep.
"""
import re
import unittest
from pathlib import Path


_REPO_ROOT = Path(__file__).resolve().parents[2]


class TestMocksChokepoint(unittest.TestCase):
    """Every public callable in race/mocks/{github,calendar,travel}.py
    must contain `inject_fault(` somewhere in its body."""

    MOCK_FILES = [
        "src/a2a_vs_mcp/race/mocks/github.py",
        "src/a2a_vs_mcp/race/mocks/calendar.py",
        "src/a2a_vs_mcp/race/mocks/travel.py",
    ]

    def _public_defs(self, src: str) -> list[str]:
        """Return list of (name, body_text) for top-level public def in src."""
        out: list[tuple[str, str]] = []
        lines = src.splitlines()
        i = 0
        while i < len(lines):
            m = re.match(r"^def ([a-zA-Z_][a-zA-Z0-9_]*)\(", lines[i])
            if m and not m.group(1).startswith("_"):
                start = i
                i += 1
                while i < len(lines) and (lines[i].startswith(" ") or lines[i].startswith("\t") or lines[i].strip() == ""):
                    i += 1
                out.append((m.group(1), "\n".join(lines[start:i])))
                continue
            i += 1
        return [(n, b) for n, b in out]

    def test_every_public_mock_callable_calls_inject_fault(self):
        for rel in self.MOCK_FILES:
            src = (_REPO_ROOT / rel).read_text()
            defs = [(n, b) for n, b in [(n, b) for n, b in []]]  # pragma: no cover - replaced below
            defs = []
            lines = src.splitlines()
            i = 0
            while i < len(lines):
                m = re.match(r"^def ([a-zA-Z_][a-zA-Z0-9_]*)\(", lines[i])
                if m and not m.group(1).startswith("_"):
                    start = i
                    i += 1
                    while i < len(lines) and (lines[i].startswith(" ") or lines[i].startswith("\t") or lines[i].strip() == ""):
                        i += 1
                    defs.append((m.group(1), "\n".join(lines[start:i])))
                    continue
                i += 1
            self.assertGreater(len(defs), 0, f"{rel}: expected ≥1 public callable")
            for name, body in defs:
                self.assertIn("inject_fault(", body, f"{rel}::{name} missing inject_fault() call — IRON RULE D-25 breach")


class TestMcpServersRouteThroughMocks(unittest.TestCase):
    """Every @mcp.tool() in mcp_servers/race_*.py must import + call into race.mocks.*."""

    SERVER_FILES = [
        "src/a2a_vs_mcp/mcp_servers/race_github.py",
        "src/a2a_vs_mcp/mcp_servers/race_calendar.py",
        "src/a2a_vs_mcp/mcp_servers/race_travel.py",
    ]

    def test_each_server_imports_race_mocks(self):
        for rel in self.SERVER_FILES:
            src = (_REPO_ROOT / rel).read_text()
            self.assertRegex(src, r"from\s+\.{1,3}race\.mocks", f"{rel} does not import race.mocks")

    def test_each_mcp_tool_routes_through_mock_module(self):
        for rel in self.SERVER_FILES:
            src = (_REPO_ROOT / rel).read_text()
            tools = re.findall(r"@mcp\.tool\(\)\s*\ndef\s+(\w+)\(", src)
            self.assertGreater(len(tools), 0, f"{rel}: expected ≥1 @mcp.tool()")
            module_name = Path(rel).stem.replace("race_", "")
            self.assertIn(f"{module_name}_mock.", src + " " + src.replace(f"from ..race.mocks import {module_name}", f"{module_name}_mock"), f"{rel}: tools must route through race.mocks.{module_name}")


if __name__ == "__main__":
    unittest.main()
```

Note: the second test's last assertion is a relaxed shape check; the actual implementation check is "tools call into the mock module by any reasonable import name." If the server uses `from ..race.mocks import github as github_mock` then `github_mock.` appears in tool bodies; if it uses `from ..race.mocks.github import get_repo_metadata` then function name appears directly. Both are acceptable — the grep gate only fails if NO race.mocks reference is present at all.
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && pytest tests/race/test_mocks_chokepoint.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/race/test_mocks_chokepoint.py -x -q` exits 0.
    - `grep -c "inject_fault(" tests/race/test_mocks_chokepoint.py` outputs at least `1`.
    - `grep -c "race.mocks" tests/race/test_mocks_chokepoint.py` outputs at least `1`.
    - `grep -c "@mcp.tool" tests/race/test_mocks_chokepoint.py` outputs at least `1`.
  </acceptance_criteria>
  <done>RACE-07 + D-25 IRON RULE source grep gate; extends Phase 6 D-13.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: Full pytest run; verify ≥ 49 tests green; commit</name>
  <read_first>
    - tests/race/ (all test files from Tasks 1-7)
  </read_first>
  <action>
1. `pytest tests/race/ -x -q` — must exit 0.
2. `pytest tests/race/ --collect-only -q | tail -1` — must show ≥ 49 tests.
3. `pytest tests/ -x -q` — must exit 0 (no regression in other suites).
4. Commit: `git add tests/race/ && git commit -m "test(07-11): land Phase 7 test suite — 12 files + fixtures"` (executor uses standard commit pattern).
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && pytest tests/race/ -x -q && pytest tests/race/ --collect-only -q | tail -1 | grep -E "[0-9]+\s*tests" | awk '{print $1}' | xargs -I{} test {} -ge 49</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/race/ -x -q` exits 0.
    - Collected test count ≥ 49.
    - `pytest tests/ -x -q` exits 0 (no regression in non-race suites).
    - Verify-automated block exits 0.
  </acceptance_criteria>
  <done>Phase 7 test suite green; ≥ 49 race tests; no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures ↔ git | regex_corpus + 9 trace fixtures checked in; ground truth |
| Test code ↔ Anthropic SDK | mocked at `anthropic.AsyncAnthropic` constructor; never makes real API call |
| Test code ↔ Detector class | replay-symmetry test asserts identical class instance type used live + replay |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11-01 | Information Disclosure | hardcoded API keys in fixtures | mitigate | mock anthropic client at constructor; never check in real keys; assert exception on missing key does NOT include key value |
| T-07-11-02 | Tampering | regex corpus drift over time | mitigate | corpus checked into git as ground truth; `version` field stamps when last edited; FP target locked at 10% |
| T-07-11-03 | Tampering | replay-symmetry violation (D-33) — Detector emits different tag in test vs runtime | mitigate | `test_replay_yields_same_tag_as_live` reuses the SAME `Detector` class instance type live and over recorded events; identical tag asserted |
| T-07-11-04 | Tampering | source-grep gate bypass — someone adds a public mock callable that skips inject_fault() | mitigate | `test_every_public_mock_callable_calls_inject_fault` enumerates public defs at test time; `pytest --collect-only` includes the method, so CI catches |
</threat_model>

<verification>
- `pytest tests/race/ -x -q` exits 0 with ≥ 49 tests collected.
- `pytest tests/ -x -q` exits 0 (full suite no regression).
- Every RACE-01..07 maps to ≥1 named test method (RaceReq → test traceability table at top of plan).
- D-25 (single fault chokepoint), D-30 (hardness coverage matrix), D-33 (replay symmetry), D-36 (regex FP < 10%), D-38 (retry classifier), D-43 (negotiate_meeting structural-only) — each has at least one named test method enforcing it.
</verification>

<success_criteria>
- All 7 RACE requirements covered with grep-verifiable test methods.
- Phase 7 IRON RULES (D-25, D-30, D-33, D-36, D-38, D-43) all have explicit test methods.
- 9 trace fixtures + 50-sample regex corpus committed; deterministic ground truth for Detector + replay-symmetry tests.
- `pytest tests/race/` green; ≥ 49 tests collected.
- No regression in `pytest tests/` overall.
</success_criteria>

<output>
After completion, create `.planning/phases/07-race-backend-lanes-harness-recovery/07-11-SUMMARY.md`.
</output>
