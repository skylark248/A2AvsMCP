---
phase: 07-race-backend-lanes-harness-recovery
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/a2a_vs_mcp/race/classifier.py
autonomous: true
requirements: [RACE-04, RACE-06]
tags: [classifier, state-machine, regex, recovery]

must_haves:
  truths:
    - "race/classifier.py defines K_DEFAULT = 3 and DetectorState enum (WAITING, AWAITING_OBSERVATION, OBSERVED)"
    - "Compiled-once regexes _ACK_FAULT_REGEX, _NEGATION_TOKENS, _NEGATION_FAULT_TOKENS, _SENTENCE_SPLIT live at module scope (D-36 locked verbatim)"
    - "is_acknowledging_fault(text) implements sentence-split + negation-guard per D-36"
    - "Detector dataclass (D-31) has fault_id, fault_kind, target, fault_inject_turn, K, state, t_observed_ms, evidence_kind"
    - "Detector.consume(event) returns True on transition to OBSERVED; False otherwise"
    - "Detector.finalize_at_done(score_pass) returns one of recovered, gave_up, kept_going_without_noticing, kept_going_to_failure, indeterminate per master design terminal-state rules"
    - "Detector.finalize_at_race_done_no_done() returns indeterminate per D-34"
    - "failure_mode_classifier(lane, task_id, per_run_tags, agg) returns one of 6 templates (recovered, gave_up, kept_going_without_noticing, kept_going_to_failure, indeterminate, lane_failed) per D-35"
    - "_dominant_tag implements precedence rule recovered > gave_up > kept_going_without_noticing > kept_going_to_failure > indeterminate"
    - "_characteristic_event_phrase emits per-lane phrase for pure_mcp / pure_a2a / hybrid with median fallback per D-37"
  artifacts:
    - path: "src/a2a_vs_mcp/race/classifier.py"
      provides: "Detector + failure_mode_classifier + locked regex"
      contains: "class Detector"
      min_lines: 200
  key_links:
    - from: "src/a2a_vs_mcp/race/classifier.py::Detector"
      to: "src/a2a_vs_mcp/race/runners/* (Plan 09)"
      via: "runner instantiates Detector per fault_injected; calls consume() on each subsequent event"
      pattern: "Detector\\("
    - from: "src/a2a_vs_mcp/race/classifier.py::failure_mode_classifier"
      to: "src/a2a_vs_mcp/race/harness.py (Plan 10)"
      via: "harness invokes after race_done to populate per-lane headlines"
      pattern: "failure_mode_classifier"
---

<objective>
Wave 2 recovery brain. Land `race/classifier.py` — the K=3 stateful Detector, the locked-verbatim acknowledging-fault regex with negation guard, and the 6-template failure_mode_classifier headline generator. This module IS the recovery state machine for the entire race demo (D-31, D-35).

Purpose: RACE-04 (recovery state machine, K=3 turn window, agent_msg_acknowledging_fault regex with negation guard) and RACE-06 (6 deterministic headline templates) are satisfied here. Replay symmetry by construction (D-33): the same `Detector` class is used live in runners AND on replay over recorded events — same algorithm, same tags. Phase 9 HEAT-03 fixture test will assert this.

Output: One new module `src/a2a_vs_mcp/race/classifier.py`. ~200 LOC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-race-backend-lanes-harness-recovery/07-CONTEXT.md
@.planning/phases/07-race-backend-lanes-harness-recovery/07-RESEARCH.md
@.planning/phases/07-race-backend-lanes-harness-recovery/07-PATTERNS.md
@src/a2a_vs_mcp/race/turn.py
@src/a2a_vs_mcp/race/failure.py

<interfaces>
RESEARCH §6 lines 760-955 contains the FULL locked code for this module. The implementation below is sourced VERBATIM from that section (D-31, D-32, D-34, D-35, D-36, D-37 all reference master design content that RESEARCH already extracted). Executor copies the code from RESEARCH §6 directly — no design choices remain.

Key constants (D-36, locked):
- K_DEFAULT = 3
- _ACK_FAULT_REGEX: error/errors/errored/fail/failed/failing/failure/429/5xx/timeout/timed.out/retry/retrying/retried/unable/cannot/couldn't/didn't/stale/invalid/malformed/truncated/partial/rate.limit/unauthorized/forbidden/missing field/unexpected/unparseable
- _NEGATION_TOKENS: no/not/without/never/isn't/wasn't/aren't
- _NEGATION_FAULT_TOKENS: error/fail/issue/problem/fault/429/5xx/timeout/retry
- _SENTENCE_SPLIT: [.!?]

Negation guard: if a sentence matches _ACK_FAULT_REGEX BUT also contains BOTH _NEGATION_TOKENS and _NEGATION_FAULT_TOKENS, drop the match (handles "I cannot retry this 429" - which IS an acknowledgment, but Reviewer Concern #1 in master design accepts the false-negative; FN target is monitored alongside FP).

Detector terminal-state table (D-34 + master design §Per-fault state machine):

| Detector state at done | score_pass | terminal tag |
|------------------------|------------|--------------|
| OBSERVED               | True       | recovered |
| OBSERVED               | False      | gave_up |
| AWAITING_OBSERVATION   | True       | kept_going_without_noticing |
| AWAITING_OBSERVATION   | False      | kept_going_to_failure |
| any                    | (race_done before done) | indeterminate |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create race/classifier.py with Detector state machine and module-load regexes</name>
  <read_first>
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-RESEARCH.md §6 lines 760-855 (Detector class shape and DetectorState enum, locked verbatim)
    - src/a2a_vs_mcp/race/turn.py (module-level constant + helper-fn idiom)
    - src/a2a_vs_mcp/race/failure.py (StrEnum analog at lines 27-32)
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-PATTERNS.md classifier section (lines 268-295)
  </read_first>
  <action>
Create `src/a2a_vs_mcp/race/classifier.py`. Module structure (mandatory order):

1. First line: `from __future__ import annotations`
2. Module docstring referencing D-31, D-33, D-36, master design §Recovery detection.
3. Imports: dataclasses (dataclass), enum (Enum), re, statistics, typing (Any, Literal).
4. Constant: `K_DEFAULT: int = 3`
5. Class `DetectorState(str, Enum)` with members `WAITING = "waiting"`, `AWAITING_OBSERVATION = "awaiting_observation"`, `OBSERVED = "observed"`.
6. Module-level compiled regexes (D-36, copy verbatim from RESEARCH §6 lines 774-787):

```
_ACK_FAULT_REGEX = re.compile(
    r"\b(error|errors|errored|fail|failed|failing|failure|429|5\d\d|timeout|timed.out|"
    r"retry|retrying|retried|unable|cannot|couldn[' ]?t|did[' ]?not|didn[' ]?t|didnt|"
    r"stale|invalid|malformed|truncat\w+|partial|rate.?limit\w*|unauthorized|forbidden|"
    r"missing.\w*field|unexpected|unparse\w+)\b",
    re.IGNORECASE,
)
_NEGATION_TOKENS = re.compile(
    r"\b(no|not|without|never|isn[' ]?t|wasn[' ]?t|aren[' ]?t)\b", re.IGNORECASE,
)
_NEGATION_FAULT_TOKENS = re.compile(
    r"\b(error|fail|issue|problem|fault|429|5\d\d|timeout|retry)\b", re.IGNORECASE,
)
_SENTENCE_SPLIT = re.compile(r"[.!?]")
```

7. Function `is_acknowledging_fault(text: str) -> bool` per D-36 — sentence-split, fault-token match, negation-guard drop:

```
def is_acknowledging_fault(text: str) -> bool:
    """D-36: regex with negation guard. Sentence boundary = [.!?] or end-of-message."""
    sentences = _SENTENCE_SPLIT.split(text) if text else []
    for sent in sentences:
        if not _ACK_FAULT_REGEX.search(sent):
            continue
        if _NEGATION_TOKENS.search(sent) and _NEGATION_FAULT_TOKENS.search(sent):
            continue
        return True
    return False
```

8. `@dataclass class Detector:` per D-31 (RESEARCH §6 lines 803-812):

Fields exactly: `fault_id: str`, `fault_kind: str`, `target: str`, `fault_inject_turn: int`, `K: int = K_DEFAULT`, `state: DetectorState = DetectorState.AWAITING_OBSERVATION`, `t_observed_ms: int | None = None`, `evidence_kind: str | None = None`.

Methods:
- `consume(self, event: dict) -> bool`: implements D-32 — feed an event, transition to OBSERVED if criteria met, return True iff this call flipped state. Logic per RESEARCH §6 lines 815-832: window check (cur_turn - fault_inject_turn > K → return False), then 3 observation paths: tool_call+status==error → "tool_error"; tool_call+tool_name==target+turn>fault_inject_turn → "retry"; agent_msg with is_acknowledging_fault(content) → "agent_msg".
- `_observe(self, event, evidence_kind) -> bool`: state ← OBSERVED, capture t_observed_ms (event.get('t_ms') or event.get('t_call_ms') or 0), evidence_kind ← evidence_kind, return True.
- `finalize_at_done(self, score_pass: bool) -> str`: implements D-34 terminal-state rules — if state==OBSERVED and score_pass: "recovered"; if state==OBSERVED and not score_pass: "gave_up"; if state==AWAITING_OBSERVATION and score_pass: "kept_going_without_noticing"; if state==AWAITING_OBSERVATION and not score_pass: "kept_going_to_failure"; else: "indeterminate".
- `finalize_at_race_done_no_done(self) -> str`: returns "indeterminate" (D-34).

9. After Detector, the headline classifier per D-35/D-37 (RESEARCH §6 lines 891-955):

```
LANE = Literal["pure_mcp", "pure_a2a", "hybrid"]


def failure_mode_classifier(
    lane: LANE,
    task_id: str,
    per_run_tags: list[str],
    agg: dict[str, Any],
) -> str:
    """6 templates, locked verbatim from master design §failure_mode_classifier."""
    n = len(per_run_tags)
    if n == 0 or all(t == "lane_failed" for t in per_run_tags):
        return _template_lane_failed(lane, task_id, agg)
    dominant = _dominant_tag(per_run_tags)
    fault_summary = _fault_summary(task_id)
    if dominant == "recovered":
        return f"{lane} *recovered {agg['recovery_rate']*n:.0f}/{n} times* after {fault_summary}; avg {agg['mean_wasted_tokens']:.0f} wasted tokens."
    if dominant == "gave_up":
        return f"{lane} noticed the {fault_summary} but *gave up at turn {agg.get('median_give_up_turn', 0)}*; recovery {agg['recovery_rate']*100:.0f}%."
    if dominant == "kept_going_without_noticing":
        phrase = _characteristic_event_phrase(lane, agg)
        return f"{lane} *{phrase}* after {fault_summary}; recovery {agg['recovery_rate']*100:.0f}%."
    if dominant == "kept_going_to_failure":
        return f"{lane} *kept going past {fault_summary}* and failed the score gate; recovery 0%."
    if dominant == "indeterminate":
        ind = sum(1 for t in per_run_tags if t == "indeterminate")
        return f"{lane} produced *{ind}/{n} indeterminate runs* on this task — disclosed."
    raise ValueError(f"Unknown dominant tag: {dominant}")


def _template_lane_failed(lane: str, task_id: str, agg: dict[str, Any]) -> str:
    reason = agg.get("lane_failed_reason", "infra error")
    return f"{lane} could not complete {task_id}: {reason}."


def _dominant_tag(tags: list[str]) -> str:
    PRECEDENCE = ["recovered", "gave_up", "kept_going_without_noticing", "kept_going_to_failure", "indeterminate"]
    counts = {t: tags.count(t) for t in PRECEDENCE}
    max_count = max(counts.values()) if counts else 0
    for t in PRECEDENCE:
        if counts.get(t, 0) == max_count:
            return t
    return "indeterminate"


def _fault_summary(task_id: str) -> str:
    """Per-task short clause used in headlines."""
    return {
        "summarize_repo": "rate-limit + schema drift on the GitHub mock",
        "negotiate_meeting": "schema drift on calendar windows",
        "book_travel": "partial commit on booking + rate-limit on hotel search",
    }.get(task_id, f"fault on {task_id}")


def _characteristic_event_phrase(lane: str, agg: dict[str, Any]) -> str:
    """D-37 lookup. Counts derived from trace at headline-render time, NOT stored."""
    if lane == "pure_mcp":
        n_retries = agg.get("median_retries")
        tool = agg.get("characteristic_tool", "the_tool")
        if n_retries is not None:
            return f"retried {tool} {n_retries} times"
    elif lane == "pure_a2a":
        n_delegations = agg.get("median_delegations")
        if n_delegations is not None:
            return f"delegated {n_delegations} times"
    elif lane == "hybrid":
        n_switches = agg.get("median_switches")
        if n_switches is not None:
            return f"switched protocol path {n_switches} times"
    n_turns = agg.get("median_turns_after_fault", 0)
    return f"continued for {n_turns} turns"
```

CRITICAL constraints:
- Regex must be compiled ONCE at module load (D-36) — at module scope, not inside a function.
- Detector defaults `state = DetectorState.AWAITING_OBSERVATION` (NOT WAITING) per RESEARCH §6 — runner only constructs a Detector after seeing fault_injected, so AWAITING_OBSERVATION is the correct initial state. WAITING is reserved for "no fault scripted yet".
- `failure_mode_classifier` must NEVER call live time, randomness, or LLM. Pure function over `per_run_tags` + `agg`.
- All 6 template strings use the literal asterisks `*...*` for italics — these are markdown italics in the rendered banner clause.
- Do NOT add any logging, retries, or side effects in this module.
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && python3 -c "
from a2a_vs_mcp.race.classifier import (
    K_DEFAULT, DetectorState, Detector, is_acknowledging_fault,
    failure_mode_classifier, _dominant_tag, _characteristic_event_phrase,
)
assert K_DEFAULT == 3
assert DetectorState.OBSERVED.value == 'observed'
assert is_acknowledging_fault('I got a 429 error.') is True
assert is_acknowledging_fault('Everything is fine.') is False
assert is_acknowledging_fault('No error, all good.') is False
assert is_acknowledging_fault('I cannot complete this task.') is True
d = Detector(fault_id='f1', fault_kind='rate_limit_429', target='github_api.get_repo_metadata', fault_inject_turn=2)
assert d.state == DetectorState.AWAITING_OBSERVATION
flipped = d.consume({'event_type': 'tool_call', 'status': 'error', 'turn_index': 3, 't_ms': 100})
assert flipped is True and d.state == DetectorState.OBSERVED and d.evidence_kind == 'tool_error'
assert d.finalize_at_done(True) == 'recovered'
assert d.finalize_at_done(False) == 'gave_up'
d2 = Detector(fault_id='f2', fault_kind='partial_json', target='x', fault_inject_turn=0)
assert d2.finalize_at_done(True) == 'kept_going_without_noticing'
assert d2.finalize_at_done(False) == 'kept_going_to_failure'
assert d2.finalize_at_race_done_no_done() == 'indeterminate'
agg = {'recovery_rate': 0.6, 'mean_wasted_tokens': 1234, 'median_retries': 4, 'characteristic_tool': 'github_api.get_repo_metadata'}
h = failure_mode_classifier('pure_mcp', 'summarize_repo', ['recovered','recovered','recovered','gave_up','indeterminate'], agg)
assert 'pure_mcp' in h and 'recovered' in h.lower()
h_kgwn = failure_mode_classifier('pure_mcp', 'summarize_repo', ['kept_going_without_noticing']*3, agg)
assert 'retried github_api.get_repo_metadata 4 times' in h_kgwn
h_lane = failure_mode_classifier('hybrid', 'book_travel', ['lane_failed']*3, {'lane_failed_reason': 'judge timeout'})
assert 'could not complete' in h_lane
assert _dominant_tag(['recovered','gave_up','recovered']) == 'recovered'
assert _dominant_tag(['gave_up','recovered']) == 'recovered'  # precedence on tie
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^from __future__ import annotations" src/a2a_vs_mcp/race/classifier.py` returns line 1.
    - `grep -c "K_DEFAULT" src/a2a_vs_mcp/race/classifier.py` outputs at least `1`.
    - `grep -c "class DetectorState(str, Enum)" src/a2a_vs_mcp/race/classifier.py` outputs `1`.
    - `grep -c "_ACK_FAULT_REGEX = re.compile" src/a2a_vs_mcp/race/classifier.py` outputs `1` (compiled at module scope, NOT inside a function).
    - `grep -c "_NEGATION_TOKENS = re.compile" src/a2a_vs_mcp/race/classifier.py` outputs `1`.
    - `grep -c "@dataclass" src/a2a_vs_mcp/race/classifier.py` outputs at least `1` (Detector).
    - `grep -c "def consume" src/a2a_vs_mcp/race/classifier.py` outputs `1`.
    - `grep -c "def finalize_at_done" src/a2a_vs_mcp/race/classifier.py` outputs `1`.
    - `grep -c "def failure_mode_classifier" src/a2a_vs_mcp/race/classifier.py` outputs `1`.
    - `grep -c "def _dominant_tag" src/a2a_vs_mcp/race/classifier.py` outputs `1`.
    - `grep -c "def _characteristic_event_phrase" src/a2a_vs_mcp/race/classifier.py` outputs `1`.
    - All 5 terminal tags appear as string literals: `grep -c '"recovered"\|"gave_up"\|"kept_going_without_noticing"\|"kept_going_to_failure"\|"indeterminate"' src/a2a_vs_mcp/race/classifier.py` outputs at least `5`.
    - `wc -l src/a2a_vs_mcp/race/classifier.py | awk '{print $1}'` ≥ 150.
    - Quick smoke test (verify automated block) exits 0.
    - `pytest tests/race/ -x -q` (Phase 6 + Plan 02 regression) exits 0.
  </acceptance_criteria>
  <done>classifier.py implements Detector + failure_mode_classifier with locked regex, terminal-state rules, and 6 templates; smoke test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trace event ↔ Detector | Untrusted (replay path may load adversarial fixture) |
| LLM agent message ↔ regex classifier | LLM output is untrusted text fed into compiled regex |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | regex drift after merge | mitigate | Regex strings are checked into git; Plan 11 `test_classifier_regex.py` runs a 50-sample corpus to catch FP/FN regression |
| T-07-04-02 | Denial of Service | catastrophic regex backtracking on malicious agent_msg | mitigate | Patterns use bounded `\b...\b` word-boundary anchors and limited `\w+` quantifiers — no nested unbounded alternation |
| T-07-04-03 | Tampering | replay-symmetry violation (Detector behaves differently on replay) | mitigate | Same `Detector` class consumed by both live runners (Plan 09) and replay path (Phase 9 HEAT-03 fixture); D-33 by construction |
| T-07-04-04 | Information Disclosure | classifier headlines leak run content | accept | Headlines describe run shape (counts/turns/tags), not user content |
</threat_model>

<verification>
- `pytest tests/race/ -x -q` (Phase 6 + Plan 02 regression) exits 0.
- All 11 acceptance criteria via grep pass.
- Smoke-test verify command exits 0 — exercises Detector lifecycle, terminal-state rules, all 4 lane phrases, dominant-tag precedence.
- Plan 11 `test_classifier_detector.py` and `test_classifier_regex.py` later assert the K=3 window, 9-fixture snapshot, 50-sample regex corpus.
</verification>

<success_criteria>
- RACE-04: Detector implements K=3 turn window + locked regex with negation guard; terminal-state table matches master design.
- RACE-06: failure_mode_classifier emits exactly the 6 documented templates; replay symmetry guaranteed by single class shared with replay path.
- D-33 (replay symmetry by construction) — same Detector reused; Phase 9 HEAT-03 will assert via fixture.
</success_criteria>

<output>
After completion, create `.planning/phases/07-race-backend-lanes-harness-recovery/07-04-SUMMARY.md`.
</output>
