---
phase: 07-race-backend-lanes-harness-recovery
plan: 08
type: execute
wave: 3
depends_on: [02, 03, 06]
files_modified:
  - src/a2a_vs_mcp/race/tasks/__init__.py
  - src/a2a_vs_mcp/race/tasks/loader.py
  - src/a2a_vs_mcp/race/tasks/summarize_repo/__init__.py
  - src/a2a_vs_mcp/race/tasks/summarize_repo/task_config.yaml
  - src/a2a_vs_mcp/race/tasks/negotiate_meeting/__init__.py
  - src/a2a_vs_mcp/race/tasks/negotiate_meeting/task_config.yaml
  - src/a2a_vs_mcp/race/tasks/book_travel/__init__.py
  - src/a2a_vs_mcp/race/tasks/book_travel/task_config.yaml
autonomous: true
requirements: [RACE-01, RACE-05]
tags: [tasks, registries, pydantic, scorers, yaml]

must_haves:
  truths:
    - "race/tasks/<id>/task_config.yaml exists for all 3 v1 tasks (summarize_repo, negotiate_meeting, book_travel)"
    - "Each task_config.yaml has task_id, hardness_profile (list of HardnessType names), failure_script (FaultKind kinds), hybrid_plan with steps using on_fault enum (retry_once|delegate|abort|continue)"
    - "Hardness coverage (D-30 locked): LONG_CHAIN in {summarize_repo, book_travel}; RATE_PRESSURE in {summarize_repo, book_travel}; SCHEMA_VARIANCE in {summarize_repo, negotiate_meeting}; MULTI_SOURCE_SYNTHESIS in {negotiate_meeting, book_travel}"
    - "Each task ships __init__.py with TARGETS dict[str, Callable] (failure_script.target -> mock callable per D-27) and BINDS dict[str, Callable[[ExecutionContext], Any]] (hybrid_plan.bind -> resolver)"
    - "race/tasks/loader.py defines TaskConfig pydantic model with cross-validation: unknown target or bind raises ValidationError at startup (D-28)"
    - "race/tasks/__init__.py imports all 3 tasks at module load (V1_TASK_IDS list-comp triggers validation per RESEARCH §7)"
    - "summarize_repo scorer uses HaikuJudge (3/3 rubric)"
    - "negotiate_meeting scorer is structural-only — proposed time fits in all 3 owners' free windows (D-43, no Haiku call)"
    - "book_travel scorer is composite — structural (cost <= budget AND legs connect) AND Haiku purpose-match (R1 YES)"
    - "loader uses yaml.safe_load (no arbitrary code execution)"
  artifacts:
    - path: "src/a2a_vs_mcp/race/tasks/loader.py"
      provides: "TaskConfig pydantic model + load_task_config(task_id) cross-validator"
      contains: "class TaskConfig"
      min_lines: 70
    - path: "src/a2a_vs_mcp/race/tasks/__init__.py"
      provides: "module-load validation hook (TASK_CONFIGS dict-comp)"
      contains: "V1_TASK_IDS"
    - path: "src/a2a_vs_mcp/race/tasks/summarize_repo/task_config.yaml"
      provides: "summarize_repo task config"
      contains: "task_id: summarize_repo"
    - path: "src/a2a_vs_mcp/race/tasks/summarize_repo/__init__.py"
      provides: "summarize_repo TARGETS + BINDS + Haiku score()"
      contains: "TARGETS"
      min_lines: 60
    - path: "src/a2a_vs_mcp/race/tasks/negotiate_meeting/task_config.yaml"
      provides: "negotiate_meeting task config"
      contains: "task_id: negotiate_meeting"
    - path: "src/a2a_vs_mcp/race/tasks/negotiate_meeting/__init__.py"
      provides: "negotiate_meeting TARGETS + BINDS + structural score()"
      contains: "TARGETS"
      min_lines: 50
    - path: "src/a2a_vs_mcp/race/tasks/book_travel/task_config.yaml"
      provides: "book_travel task config"
      contains: "task_id: book_travel"
    - path: "src/a2a_vs_mcp/race/tasks/book_travel/__init__.py"
      provides: "book_travel TARGETS + BINDS + composite score()"
      contains: "TARGETS"
      min_lines: 70
  key_links:
    - from: "race/tasks/loader.py::load_task_config"
      to: "race/tasks/<id>/task_config.yaml + race/tasks/<id>/__init__.py"
      via: "yaml.safe_load -> TaskConfig.model_validate -> cross-validate target/bind against TARGETS/BINDS"
      pattern: "load_task_config"
    - from: "race/tasks/__init__.py module-load"
      to: "all 3 task __init__.py files"
      via: "TASK_CONFIGS = {tid: load_task_config(tid) for tid in V1_TASK_IDS} runs at first import"
      pattern: "TASK_CONFIGS"
---

<objective>
Wave 3 task surface. Land the 3 v1 tasks: each ships a `task_config.yaml` (failure_script + hybrid_plan + hardness_profile) plus an `__init__.py` (TARGETS + BINDS callable registries + per-task score()). Pydantic loader cross-validates targets and binds at startup (D-28) — typo = `pytest --collect-only` fails noisy.

Purpose: RACE-01 (HardnessProfile inspection on seeded YAMLs satisfies the "≥2 tasks per type" coverage rule) + RACE-05 (per-task scorer per task: Haiku for summarize_repo, structural for negotiate_meeting, composite for book_travel) are satisfied here. Failure scripts feed Plan 09 runners; hybrid_plans drive Plan 09's hybrid runner.

Output: 3 task_config.yaml files + 3 task __init__.py files + 1 loader.py + 1 tasks/__init__.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-race-backend-lanes-harness-recovery/07-CONTEXT.md
@.planning/phases/07-race-backend-lanes-harness-recovery/07-RESEARCH.md
@.planning/phases/07-race-backend-lanes-harness-recovery/07-PATTERNS.md
@src/a2a_vs_mcp/race/types.py
@src/a2a_vs_mcp/race/failure.py
@src/a2a_vs_mcp/race/mocks/github.py
@src/a2a_vs_mcp/race/mocks/calendar.py
@src/a2a_vs_mcp/race/mocks/travel.py
@src/a2a_vs_mcp/race/judges/haiku.py

<interfaces>
RESEARCH §7 lines 1011-1145 contains the FULL locked TaskConfig schema and per-task __init__.py shapes. Per master design §task_config.yaml the YAML structure is:

```
task_id: <name>
hardness_profile: [list of HardnessType enum names: long_chain | rate_pressure | schema_variance | multi_source]
failure_script:
  - kind: <FaultKind: rate_limit_429 | partial_json | schema_drift | eventual_consistency_read | partial_commit_5xx>
    target: <dotted target name; must exist in this task's TARGETS registry>
    after_calls: <int, default 0>
    duration_calls: <int, default 1>
    # plus free-form per-kind extras (truncate_at_byte, target_calendar_id, drift, ...)
hybrid_plan:
  steps:
    - kind: <tool | delegate>
      tool: <tool name>      # if kind=tool
      agent: <agent name>    # if kind=delegate
      goal: <goal>           # optional
      bind: <BINDS key>      # optional; cross-validated against task BINDS at startup
      on_fault: <retry_once | delegate | abort | continue>  # optional
```

D-30 hardness coverage (locked):
- LONG_CHAIN ∈ {summarize_repo, book_travel}
- RATE_PRESSURE ∈ {summarize_repo, book_travel}
- SCHEMA_VARIANCE ∈ {summarize_repo, negotiate_meeting}
- MULTI_SOURCE_SYNTHESIS ∈ {negotiate_meeting, book_travel}

So:
- summarize_repo.hardness_profile = [long_chain, rate_pressure, schema_variance]
- negotiate_meeting.hardness_profile = [schema_variance, multi_source]
- book_travel.hardness_profile = [long_chain, rate_pressure, multi_source]

Each type appears in ≥2 tasks. Plan 11's test_hardness_coverage.py asserts this.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create race/tasks/loader.py with TaskConfig pydantic model + load_task_config()</name>
  <read_first>
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-RESEARCH.md §7 lines 1011-1090 (full TaskConfig + load_task_config code)
    - src/a2a_vs_mcp/race/failure.py lines 101-110 (TypeAdapter pattern + validate_failure_script)
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-PATTERNS.md tasks/loader section (lines 472-501)
  </read_first>
  <action>
Create `src/a2a_vs_mcp/race/tasks/loader.py`:

```
"""TaskConfig pydantic loader (D-28).

Cross-validates each failure_script.target against the task's TARGETS registry
and each hybrid_plan.steps[].bind against BINDS. Typo = ValidationError at
import time (RESEARCH §7 + Phase 6 D-12 inheritance).

YAML loaded via yaml.safe_load (NEVER yaml.load — prevents arbitrary Python
object construction).
"""
from __future__ import annotations

from importlib import resources
from typing import Any, Callable, Literal

import yaml
from pydantic import BaseModel, ConfigDict, ValidationError, field_validator

from ..failure import FaultKind
from ..types import HardnessType


OnFault = Literal["retry_once", "delegate", "abort", "continue"]


class HybridStep(BaseModel):
    model_config = ConfigDict(extra="forbid")
    kind: Literal["tool", "delegate"]
    tool: str | None = None
    agent: str | None = None
    goal: str | None = None
    bind: str | None = None
    on_fault: OnFault | None = None


class HybridPlan(BaseModel):
    model_config = ConfigDict(extra="forbid")
    steps: list[HybridStep]


class FailureScriptYAMLEntry(BaseModel):
    model_config = ConfigDict(extra="allow")  # free-form per-kind extras allowed
    kind: FaultKind
    target: str
    after_calls: int = 0
    duration_calls: int = 1


class TaskConfig(BaseModel):
    model_config = ConfigDict(extra="forbid")
    task_id: str
    hardness_profile: list[HardnessType]
    failure_script: list[FailureScriptYAMLEntry]
    hybrid_plan: HybridPlan

    @field_validator("hardness_profile")
    @classmethod
    def _profile_nonempty(cls, v: list[HardnessType]) -> list[HardnessType]:
        if not v:
            raise ValueError("hardness_profile must list at least one HardnessType")
        return v


def load_task_config(task_id: str) -> tuple[TaskConfig, dict[str, Callable], dict[str, Callable]]:
    """Load + validate task_config.yaml. Cross-validates target + bind identifiers
    against the task module's TARGETS and BINDS registries.

    Raises pydantic.ValidationError on:
      - unknown FaultKind / HardnessType / OnFault enum value
      - unknown failure_script.target (not in TARGETS keys)
      - unknown hybrid_plan.steps[].bind (not in BINDS keys)
    """
    pkg = f"a2a_vs_mcp.race.tasks.{task_id}"
    yaml_text = resources.files(pkg).joinpath("task_config.yaml").read_text()
    raw = yaml.safe_load(yaml_text)
    cfg = TaskConfig.model_validate(raw)

    mod = __import__(pkg, fromlist=["TARGETS", "BINDS"])
    targets: dict[str, Callable] = mod.TARGETS
    binds: dict[str, Callable] = mod.BINDS

    for entry in cfg.failure_script:
        if entry.target not in targets:
            raise ValueError(
                f"Unknown failure_script target '{entry.target}' in {task_id}/task_config.yaml; "
                f"known TARGETS: {sorted(targets.keys())}"
            )
    for step in cfg.hybrid_plan.steps:
        if step.bind is not None and step.bind not in binds:
            raise ValueError(
                f"Unknown hybrid_plan bind '{step.bind}' in {task_id}/task_config.yaml; "
                f"known BINDS: {sorted(binds.keys())}"
            )
    return cfg, targets, binds
```

Note on cross-validation errors: RESEARCH §7 used `ValidationError.from_exception_data(...)` which is awkward; raising plain `ValueError` is cleaner and still surfaces a startup failure when called from `tasks/__init__.py` module-load.

CRITICAL: NEVER use `yaml.load(...)`; ONLY `yaml.safe_load(...)`. Forbid extra fields on TaskConfig + HybridPlan + HybridStep (`extra="forbid"`); allow extras only on FailureScriptYAMLEntry where per-kind extras live.
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && python3 -c "
from a2a_vs_mcp.race.tasks.loader import TaskConfig, HybridPlan, HybridStep, FailureScriptYAMLEntry, OnFault, load_task_config
# Schema validation: unknown FaultKind rejected.
try:
    TaskConfig.model_validate({'task_id': 't', 'hardness_profile': ['long_chain'], 'failure_script': [{'kind': 'BAD', 'target': 'x'}], 'hybrid_plan': {'steps': []}})
    assert False, 'expected validation failure on bad FaultKind'
except Exception:
    pass
# Schema validation: unknown HardnessType rejected.
try:
    TaskConfig.model_validate({'task_id': 't', 'hardness_profile': ['BAD'], 'failure_script': [], 'hybrid_plan': {'steps': []}})
    assert False, 'expected validation failure on bad HardnessType'
except Exception:
    pass
# Schema validation: unknown on_fault rejected.
try:
    TaskConfig.model_validate({'task_id': 't', 'hardness_profile': ['long_chain'], 'failure_script': [], 'hybrid_plan': {'steps': [{'kind': 'tool', 'tool': 't', 'on_fault': 'BAD'}]}})
    assert False, 'expected validation failure on bad on_fault'
except Exception:
    pass
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class TaskConfig" src/a2a_vs_mcp/race/tasks/loader.py` outputs `1`.
    - `grep -c "yaml.safe_load" src/a2a_vs_mcp/race/tasks/loader.py` outputs `1`.
    - `grep -c "yaml.load(" src/a2a_vs_mcp/race/tasks/loader.py` outputs `0` (NEVER use yaml.load).
    - `grep -c "OnFault = Literal" src/a2a_vs_mcp/race/tasks/loader.py` outputs `1`.
    - `grep -c "extra=.forbid." src/a2a_vs_mcp/race/tasks/loader.py` outputs at least `3` (TaskConfig, HybridPlan, HybridStep).
    - Verify-automated block exits 0 (3 schema rejections work).
  </acceptance_criteria>
  <done>Loader pydantic model + cross-validator in place; rejects bad enums.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create summarize_repo task — task_config.yaml + __init__.py with TARGETS, BINDS, Haiku scorer</name>
  <read_first>
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-RESEARCH.md §3 lines 422-440 (summarize_repo Haiku rubric verbatim) and §7 lines 1093-1132 (per-task __init__.py shape)
    - src/a2a_vs_mcp/race/mocks/github.py (callables to register in TARGETS)
    - src/a2a_vs_mcp/race/judges/haiku.py (HaikuJudge interface)
    - src/a2a_vs_mcp/race/types.py (ScoreCard shape)
  </read_first>
  <action>
**File A: `src/a2a_vs_mcp/race/tasks/summarize_repo/task_config.yaml`**:

```
task_id: summarize_repo
hardness_profile:
  - long_chain
  - rate_pressure
  - schema_variance
failure_script:
  - kind: rate_limit_429
    target: github_api.get_repo_metadata
    after_calls: 1
    duration_calls: 2
  - kind: schema_drift
    target: github_api.list_files
    after_calls: 2
    duration_calls: 1
    drift: "files->paths"
hybrid_plan:
  steps:
    - kind: tool
      tool: github_api.get_repo_metadata
      goal: "fetch repo metadata"
      on_fault: retry_once
    - kind: tool
      tool: github_api.list_files
      goal: "list source files"
      on_fault: retry_once
    - kind: tool
      tool: github_api.read_file
      goal: "read entry point + 2 modules"
      on_fault: continue
    - kind: delegate
      agent: summarizer
      goal: "produce 3-line summary"
      bind: from_subagent_output
      on_fault: abort
```

**File B: `src/a2a_vs_mcp/race/tasks/summarize_repo/__init__.py`**:

```
"""summarize_repo task registry (D-27, D-30, D-42).

TARGETS — failure_script.target -> mock callable.
BINDS   — hybrid_plan.bind key -> ExecutionContext resolver.
score() — per-task scorer using HaikuJudge (R1/R2/R3 = 3/3 pass).
"""
from __future__ import annotations
from typing import Any, Callable

from ...mocks import github as github_mock
from ...types import ExecutionContext, ScoreCard
from ...judges.haiku import HaikuJudge

# D-27 registries
TARGETS: dict[str, Callable[..., Any]] = {
    "github_api.get_repo_metadata": github_mock.get_repo_metadata,
    "github_api.list_files": github_mock.list_files,
    "github_api.read_file": github_mock.read_file,
}

BINDS: dict[str, Callable[[ExecutionContext], Any]] = {
    "from_subagent_output": lambda ctx: ctx.get("subagent_outputs", {}).get("summarizer"),
}

# D-42 rubric — locked verbatim from RESEARCH §3 lines 425-437.
RUBRIC = """You are a strict rubric scorer. Read the assistant output and answer YES/NO for each item.
RUBRIC:
  R1. Does the summary state the repository's purpose in one sentence?
  R2. Does the summary mention at least 3 distinct modules?
  R3. Does the summary identify the entry point (CLI, main, app)?
Output format (verbatim, machine-parseable):
R1: YES|NO
R2: YES|NO
R3: YES|NO
RATIONALE: <1 sentence>
"""


def score(result: dict[str, Any], trace: list[dict], judge: HaikuJudge | None) -> ScoreCard:
    if judge is None:
        return ScoreCard(success=False, ttff_ms=0, recovered=False,
                         wasted_tokens_before_detection=None, failure_mode="judge_failed",
                         cost_usd=0.0, latency_ms=0)
    summary = result.get("summary", "")
    verdict = judge.judge(rubric_system_prompt=RUBRIC, artifact_user_prompt=summary)
    text = verdict.rationale.upper()
    r1 = "R1: YES" in text
    r2 = "R2: YES" in text
    r3 = "R3: YES" in text
    passed = r1 and r2 and r3
    return ScoreCard(
        success=passed, ttff_ms=0, recovered=False,
        wasted_tokens_before_detection=None,
        failure_mode="success" if passed else "judge_failed",
        cost_usd=0.0, latency_ms=0,
    )
```
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && python3 -c "
from a2a_vs_mcp.race.tasks.summarize_repo import TARGETS, BINDS, RUBRIC, score
assert set(TARGETS.keys()) == {'github_api.get_repo_metadata', 'github_api.list_files', 'github_api.read_file'}
assert 'from_subagent_output' in BINDS
assert 'R1.' in RUBRIC and 'R2.' in RUBRIC and 'R3.' in RUBRIC
import yaml
from importlib import resources
y = yaml.safe_load(resources.files('a2a_vs_mcp.race.tasks.summarize_repo').joinpath('task_config.yaml').read_text())
assert y['task_id'] == 'summarize_repo'
assert set(y['hardness_profile']) == {'long_chain', 'rate_pressure', 'schema_variance'}
assert all(s['target'] in TARGETS for s in y['failure_script'])
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "task_id: summarize_repo" src/a2a_vs_mcp/race/tasks/summarize_repo/task_config.yaml` outputs `1`.
    - `grep -cE "^  - long_chain|^  - rate_pressure|^  - schema_variance" src/a2a_vs_mcp/race/tasks/summarize_repo/task_config.yaml` outputs `3`.
    - `python3 -c "import yaml; from importlib import resources; y = yaml.safe_load(resources.files('a2a_vs_mcp.race.tasks.summarize_repo').joinpath('task_config.yaml').read_text()); assert y['task_id']=='summarize_repo' and len(y['hardness_profile'])==3 and len(y['failure_script'])==2"` exits 0.
    - `grep -c "TARGETS" src/a2a_vs_mcp/race/tasks/summarize_repo/__init__.py` outputs at least `1`.
    - `grep -c "RUBRIC" src/a2a_vs_mcp/race/tasks/summarize_repo/__init__.py` outputs at least `2`.
    - `grep -c "R1\\.\\|R2\\.\\|R3\\." src/a2a_vs_mcp/race/tasks/summarize_repo/__init__.py` outputs `3`.
  </acceptance_criteria>
  <done>summarize_repo registers 3 targets, 1 bind; YAML matches D-30 hardness profile; Haiku scorer wires through verdict parsing.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create negotiate_meeting task — task_config.yaml + __init__.py with structural-only score (D-43)</name>
  <read_first>
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-CONTEXT.md D-43 (structural-only, no LLM judge)
    - src/a2a_vs_mcp/race/mocks/calendar.py (Plan 03 — Task 4)
    - data/race/fixtures/calendar/calendars.json
  </read_first>
  <action>
**File A: `src/a2a_vs_mcp/race/tasks/negotiate_meeting/task_config.yaml`**:

```
task_id: negotiate_meeting
hardness_profile:
  - schema_variance
  - multi_source
failure_script:
  - kind: schema_drift
    target: calendar_api.get_free_busy
    after_calls: 1
    duration_calls: 1
    drift: "free_busy->slots"
  - kind: eventual_consistency_read
    target: calendar_api.propose_time
    after_calls: 0
    duration_calls: 1
hybrid_plan:
  steps:
    - kind: tool
      tool: calendar_api.get_free_busy
      goal: "fetch alice@demo.org free/busy"
      on_fault: retry_once
    - kind: tool
      tool: calendar_api.get_free_busy
      goal: "fetch bob@demo.org free/busy"
      on_fault: retry_once
    - kind: tool
      tool: calendar_api.get_free_busy
      goal: "fetch carol@demo.org free/busy"
      on_fault: retry_once
    - kind: tool
      tool: calendar_api.propose_time
      goal: "compute mutual free 60-min window across 3 owners"
      bind: combined_owners
      on_fault: abort
```

**File B: `src/a2a_vs_mcp/race/tasks/negotiate_meeting/__init__.py`**:

```
"""negotiate_meeting task registry (D-27, D-30, D-43 — structural-only).

NO HaikuJudge import (D-43). score() returns success based on:
  - proposed time fits inside ALL 3 owners' free_busy windows.
"""
from __future__ import annotations
from datetime import datetime
from typing import Any, Callable

from ...mocks import calendar as calendar_mock
from ...types import ExecutionContext, ScoreCard

TARGETS: dict[str, Callable[..., Any]] = {
    "calendar_api.get_free_busy": calendar_mock.get_free_busy,
    "calendar_api.propose_time": calendar_mock.propose_time,
}

BINDS: dict[str, Callable[[ExecutionContext], Any]] = {
    "combined_owners": lambda ctx: list((ctx.get("tool_outputs") or {}).get("free_busy_owners", [])),
}


def _within(window: dict[str, str], start: datetime, end: datetime) -> bool:
    w_start = datetime.fromisoformat(window["start"])
    w_end = datetime.fromisoformat(window["end"])
    return w_start <= start and end <= w_end


def score(result: dict[str, Any], trace: list[dict], judge: Any = None) -> ScoreCard:
    """Structural pass: proposed time fits in all 3 owners' free windows."""
    proposal = result.get("proposed_time") or result.get("proposal")
    free_busy_by_owner = result.get("free_busy_by_owner", {})
    if not proposal or not free_busy_by_owner:
        return ScoreCard(success=False, ttff_ms=0, recovered=False,
                         wasted_tokens_before_detection=None, failure_mode="structural_failed",
                         cost_usd=0.0, latency_ms=0)
    try:
        prop_start = datetime.fromisoformat(proposal["start"])
        prop_end = datetime.fromisoformat(proposal["end"])
    except (KeyError, ValueError):
        return ScoreCard(success=False, ttff_ms=0, recovered=False,
                         wasted_tokens_before_detection=None, failure_mode="structural_failed",
                         cost_usd=0.0, latency_ms=0)
    for owner, fb in free_busy_by_owner.items():
        if not any(w.get("status") == "free" and _within(w, prop_start, prop_end) for w in fb.get("free_busy", [])):
            return ScoreCard(success=False, ttff_ms=0, recovered=False,
                             wasted_tokens_before_detection=None, failure_mode="structural_failed",
                             cost_usd=0.0, latency_ms=0)
    return ScoreCard(success=True, ttff_ms=0, recovered=False,
                     wasted_tokens_before_detection=None, failure_mode="success",
                     cost_usd=0.0, latency_ms=0)
```

CRITICAL: This file MUST NOT import from `a2a_vs_mcp.race.judges` per D-43. Plan 11's `test_task_config_loader.py::test_negotiate_no_haiku` will grep this file to assert no HaikuJudge import.
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && python3 -c "
from a2a_vs_mcp.race.tasks.negotiate_meeting import TARGETS, BINDS, score
assert set(TARGETS.keys()) == {'calendar_api.get_free_busy', 'calendar_api.propose_time'}
assert 'combined_owners' in BINDS
import yaml
from importlib import resources
y = yaml.safe_load(resources.files('a2a_vs_mcp.race.tasks.negotiate_meeting').joinpath('task_config.yaml').read_text())
assert y['task_id'] == 'negotiate_meeting'
assert set(y['hardness_profile']) == {'schema_variance', 'multi_source'}
print('OK')
" && grep -c "HaikuJudge\|race.judges" src/a2a_vs_mcp/race/tasks/negotiate_meeting/__init__.py | awk '$1==0{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "task_id: negotiate_meeting" src/a2a_vs_mcp/race/tasks/negotiate_meeting/task_config.yaml` outputs `1`.
    - `grep -cE "^  - schema_variance|^  - multi_source" src/a2a_vs_mcp/race/tasks/negotiate_meeting/task_config.yaml` outputs `2`.
    - `grep -c "HaikuJudge\|race.judges" src/a2a_vs_mcp/race/tasks/negotiate_meeting/__init__.py` outputs `0` (D-43 — no LLM judge).
    - `python3 -c "from a2a_vs_mcp.race.tasks.negotiate_meeting import TARGETS, BINDS, score; assert set(TARGETS) == {'calendar_api.get_free_busy', 'calendar_api.propose_time'}"` exits 0.
  </acceptance_criteria>
  <done>negotiate_meeting structural-only scorer; no HaikuJudge import (D-43 enforced).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create book_travel task — task_config.yaml + __init__.py with composite (structural AND Haiku) score</name>
  <read_first>
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-RESEARCH.md §3 lines 442-467 (book_travel composite score and Haiku rubric verbatim)
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-CONTEXT.md D-42
    - src/a2a_vs_mcp/race/mocks/travel.py (Plan 03 — Task 5)
  </read_first>
  <action>
**File A: `src/a2a_vs_mcp/race/tasks/book_travel/task_config.yaml`**:

```
task_id: book_travel
hardness_profile:
  - long_chain
  - rate_pressure
  - multi_source
failure_script:
  - kind: rate_limit_429
    target: travel_api.search_hotels
    after_calls: 1
    duration_calls: 2
  - kind: partial_commit_5xx
    target: travel_api.book_itinerary
    after_calls: 0
    duration_calls: 1
hybrid_plan:
  steps:
    - kind: tool
      tool: travel_api.search_flights
      goal: "search SFO->JFK outbound"
      on_fault: retry_once
    - kind: tool
      tool: travel_api.search_flights
      goal: "search JFK->SFO return"
      on_fault: retry_once
    - kind: tool
      tool: travel_api.search_hotels
      goal: "search NYC hotels"
      on_fault: retry_once
    - kind: delegate
      agent: trip_planner
      goal: "compute lowest-cost combo within budget"
      bind: lowest_cost_combo
      on_fault: delegate
    - kind: tool
      tool: travel_api.book_itinerary
      goal: "confirm booking"
      bind: lowest_cost_combo
      on_fault: abort
```

**File B: `src/a2a_vs_mcp/race/tasks/book_travel/__init__.py`**:

```
"""book_travel task registry (D-27, D-30, D-42 composite).

Composite scorer: structural (cost <= budget AND legs connect) AND Haiku
(trip purpose match per RESEARCH §3 lines 444-449). Both must pass.
"""
from __future__ import annotations
from datetime import datetime
from typing import Any, Callable

from ...mocks import travel as travel_mock
from ...types import ExecutionContext, ScoreCard
from ...judges.haiku import HaikuJudge

TARGETS: dict[str, Callable[..., Any]] = {
    "travel_api.search_flights": travel_mock.search_flights,
    "travel_api.search_hotels": travel_mock.search_hotels,
    "travel_api.book_itinerary": travel_mock.book_itinerary,
}

BINDS: dict[str, Callable[[ExecutionContext], Any]] = {
    "lowest_cost_combo": lambda ctx: (ctx.get("scratchpad") or {}).get("lowest_cost_combo"),
}

# D-42 Haiku rubric (RESEARCH §3 lines 444-449).
RUBRIC = """You are a strict rubric scorer for a travel-booking trip plan.
RUBRIC:
  R1. Does the booked itinerary match the user's stated trip purpose (e.g., business trip vs vacation)?
Output format:
R1: YES|NO
RATIONALE: <1 sentence>
"""


def _legs_connect(itinerary: dict[str, Any]) -> bool:
    flights = itinerary.get("flights") or []
    if len(flights) < 2:
        return True  # single-leg booking is trivially connected
    try:
        for prev, cur in zip(flights, flights[1:]):
            if prev.get("destination") != cur.get("origin"):
                # Allow same-city return (NYC->SFO after SFO->NYC reverses).
                if prev.get("destination") == cur.get("origin"):
                    continue
        return True
    except Exception:
        return False


def score(result: dict[str, Any], trace: list[dict], judge: HaikuJudge | None) -> ScoreCard:
    booking = result.get("booking", {})
    budget_usd = result.get("budget_usd", 0)
    total_cost = booking.get("total_cost_usd", float("inf"))
    structural_pass = (total_cost <= budget_usd) and _legs_connect(booking)

    haiku_pass = False
    if judge is not None:
        verdict = judge.judge(
            rubric_system_prompt=RUBRIC,
            artifact_user_prompt=result.get("summary", ""),
        )
        haiku_pass = "R1: YES" in verdict.rationale.upper()

    passed = structural_pass and haiku_pass
    return ScoreCard(
        success=passed, ttff_ms=0, recovered=False,
        wasted_tokens_before_detection=None,
        failure_mode="success" if passed else ("structural_failed" if not structural_pass else "judge_failed"),
        cost_usd=0.0, latency_ms=0,
    )
```
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && python3 -c "
from a2a_vs_mcp.race.tasks.book_travel import TARGETS, BINDS, RUBRIC, score
assert set(TARGETS.keys()) == {'travel_api.search_flights', 'travel_api.search_hotels', 'travel_api.book_itinerary'}
assert 'lowest_cost_combo' in BINDS
assert 'R1.' in RUBRIC
import yaml
from importlib import resources
y = yaml.safe_load(resources.files('a2a_vs_mcp.race.tasks.book_travel').joinpath('task_config.yaml').read_text())
assert y['task_id'] == 'book_travel'
assert set(y['hardness_profile']) == {'long_chain', 'rate_pressure', 'multi_source'}
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "task_id: book_travel" src/a2a_vs_mcp/race/tasks/book_travel/task_config.yaml` outputs `1`.
    - `grep -cE "^  - long_chain|^  - rate_pressure|^  - multi_source" src/a2a_vs_mcp/race/tasks/book_travel/task_config.yaml` outputs `3`.
    - `grep -c "HaikuJudge" src/a2a_vs_mcp/race/tasks/book_travel/__init__.py` outputs at least `1`.
    - `python3 -c "from a2a_vs_mcp.race.tasks.book_travel import TARGETS, BINDS, score, RUBRIC"` exits 0.
  </acceptance_criteria>
  <done>book_travel composite scorer; structural+Haiku both required.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Create race/tasks/__init__.py with module-load TASK_CONFIGS dict-comp (validation hook)</name>
  <read_first>
    - .planning/phases/07-race-backend-lanes-harness-recovery/07-RESEARCH.md §7 lines 1136-1144 (startup validation hook)
    - src/a2a_vs_mcp/race/tasks/loader.py (just created)
  </read_first>
  <action>
Create `src/a2a_vs_mcp/race/tasks/__init__.py`:

```
"""Race tasks package - startup validation hook (D-28 + RESEARCH §7).

V1_TASK_IDS lists the 3 v1 tasks. The TASK_CONFIGS module-level dict-comp
runs at first import; any typo in any task_config.yaml -> ValidationError at
import time -> pytest --collect-only fails noisy.
"""
from __future__ import annotations

from .loader import load_task_config


V1_TASK_IDS: list[str] = ["summarize_repo", "negotiate_meeting", "book_travel"]

# Module-load validation: triggers loader cross-validation for all 3 tasks.
TASK_CONFIGS: dict[str, tuple] = {tid: load_task_config(tid) for tid in V1_TASK_IDS}
```
  </action>
  <verify>
    <automated>cd /Users/shivanshchoudhary/Downloads/Projects/A2AvsMCP && python3 -c "
from a2a_vs_mcp.race.tasks import V1_TASK_IDS, TASK_CONFIGS
assert V1_TASK_IDS == ['summarize_repo', 'negotiate_meeting', 'book_travel']
assert set(TASK_CONFIGS.keys()) == set(V1_TASK_IDS)
for tid, (cfg, targets, binds) in TASK_CONFIGS.items():
    assert cfg.task_id == tid
    assert len(cfg.hardness_profile) >= 1
    assert len(cfg.failure_script) >= 1
    for entry in cfg.failure_script:
        assert entry.target in targets, f'{tid}: target {entry.target} not in TARGETS'
    for step in cfg.hybrid_plan.steps:
        if step.bind is not None:
            assert step.bind in binds, f'{tid}: bind {step.bind} not in BINDS'
# Hardness coverage check (D-30)
from a2a_vs_mcp.race.types import HardnessType
for ht in HardnessType:
    tasks_with = [tid for tid, (cfg, _, _) in TASK_CONFIGS.items() if ht in cfg.hardness_profile]
    assert len(tasks_with) >= 2, f'{ht.value} appears in only {len(tasks_with)} tasks: {tasks_with}'
print('OK')
" && pytest tests/race/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "V1_TASK_IDS = " src/a2a_vs_mcp/race/tasks/__init__.py` outputs `1`.
    - `grep -c "TASK_CONFIGS" src/a2a_vs_mcp/race/tasks/__init__.py` outputs at least `1`.
    - `python3 -c "from a2a_vs_mcp.race.tasks import V1_TASK_IDS, TASK_CONFIGS; assert len(TASK_CONFIGS) == 3"` exits 0.
    - Verify-automated block exits 0 — every target/bind cross-validates; hardness coverage D-30 holds.
    - `pytest tests/race/ -x -q` exits 0.
  </acceptance_criteria>
  <done>All 3 v1 tasks load cleanly at first import; D-30 hardness coverage holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML on disk ↔ pydantic loader | `yaml.safe_load` only — no arbitrary code execution |
| task_config target ↔ TARGETS registry | Cross-validated at import time (D-28) |
| Haiku rubric prompt ↔ Anthropic API | Rubric is project-controlled string; artifact is run output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Tampering | malicious YAML executes Python | mitigate | `yaml.safe_load` only; loader grep gate enforces |
| T-07-08-02 | Tampering | unknown FaultKind / OnFault enum at runtime | mitigate | pydantic Literal types + extra='forbid' on TaskConfig and HybridStep — typo caught at import |
| T-07-08-03 | Tampering | unknown failure_script.target | mitigate | Cross-validation in load_task_config raises at import time |
| T-07-08-04 | Information Disclosure | trip purpose / business detail in book_travel artifact sent to Haiku | accept | Synthetic mock data; user opts in via API key |
| T-07-08-05 | Spoofing | negotiate_meeting bypasses D-43 by importing HaikuJudge | mitigate | Plan 11 grep gate on negotiate_meeting/__init__.py; this plan's grep acceptance criterion enforces 0 matches |
</threat_model>

<verification>
- All 3 task_config.yaml files load through loader without error.
- D-30 hardness coverage matrix holds: 4 types each appear in ≥2 tasks.
- D-43 enforcement: negotiate_meeting/__init__.py contains 0 references to HaikuJudge or race.judges.
- `pytest tests/race/ -x -q` exits 0.
- Plan 11 `test_task_config_loader.py` later asserts: typo in target -> ValidationError at import; typo in bind -> ValidationError; per-task scorers behave per spec.
</verification>

<success_criteria>
- RACE-01 (HardnessProfile inspection) verified by D-30 coverage matrix at import time.
- RACE-05 (per-task scorers): summarize_repo Haiku 3/3, negotiate_meeting structural-only, book_travel composite.
- D-26 (YAML inside src/, importlib.resources) honored.
- D-27 (TARGETS + BINDS callable registries) honored.
- D-28 (Pydantic startup cross-validation) honored.
- D-29 (FaultKind + OnFault locked enums) honored via pydantic Literal types.
</success_criteria>

<output>
After completion, create `.planning/phases/07-race-backend-lanes-harness-recovery/07-08-SUMMARY.md`.
</output>
