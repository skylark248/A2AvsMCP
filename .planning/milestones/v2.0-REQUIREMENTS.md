# Requirements Archive — v2.0 Race Demo + Discovery + Visualization

**Defined:** 2026-04-28
**Shipped:** 2026-05-04
**Core Value:** A side-by-side, runnable comparison that makes the differences between MCP and A2A visible — not described, not diagrammed, but live and traceable.

**Milestone goal:** Ship the Three-Lane Failure-Shape Race Demo as the new flagship surface, plus tool-discovery scenario and visualization upgrades that deepen the existing comparison story.

---

## v2.0 Requirements (all complete)

### Race Schema & Trace Foundation (TRC)

Backend trace and websocket schema upgrades the rest of the race depends on.

- [x] **TRC-01**: TraceRecorder emits `t_call_start_ms`, `tokens_in`, `tokens_out` per LLM call; `t_call_ms`, `tool_name`, `status`, `error_kind` per tool call; `t_ms`, `sender`, `recipient`, `content` per inter-agent message; queryable post-run by `(run_id, lane)` in causal order
- [x] **TRC-02**: `trace_schema_version` field added to TraceRecorder; stub no-op migrator recognizes v1.0 traces in `race/replay.py`
- [x] **TRC-03**: FailureConfig emits `fault_injected` events to TraceRecorder with `fault_id`, `fault_kind`, `target`, `t_inject_ms`; emits `fault_observed` events with `evidence`, `wasted_tokens_before_detection`, `t_observed_ms` (schema + persistence path; runtime emission of fault_observed deferred to Phase 7 per D-14)
- [x] **TRC-04**: Websocket event schema (`/api/race/ws`) supports `tick`, `tool_call`, `agent_msg`, `fault_injected`, `fault_observed`, `done`, `error`, `race_done`; every event carries `turn_index` per-lane

### Race Backend (RACE)

- [x] **RACE-01**: `HardnessType` enum with 4 v1 entries; `HardnessProfile` dataclass; each v1 type appears in ≥2 tasks
- [x] **RACE-02**: Three runners — `runners/pure_mcp.py`, `runners/pure_a2a.py`, `runners/hybrid.py` — each consumes a per-task `task_config.yaml` and returns a `RaceResult`
- [x] **RACE-03**: `harness.py` drives N parallel runs per (lane, task); demo `n=5`, dev `n=1`; deterministic `model=claude-sonnet-4-6, seed=42, temperature=0`; emits live websocket events; only retries transient infrastructure errors
- [x] **RACE-04**: Recovery state machine in `race/classifier.py` tags each fault with K=3 turn window; `agent_msg_acknowledging_fault` regex with negation guard
- [x] **RACE-05**: Three v1 tasks — `summarize_repo`, `negotiate_meeting`, `book_travel` — with full `task_config.yaml` and per-task scorer (Haiku judge / structural / composite)
- [x] **RACE-06**: `failure_mode_classifier(...)` produces deterministic per-lane headline sentences from 6 templates
- [x] **RACE-07**: Mock APIs for the 3 v1 tasks — GitHub mock, calendar mock, travel mock

**Integration (gap closure):** RACE-01..07 wired via `POST /api/race/run` HTTP trigger (Phase 14-01); `useRaceStream` correctly passes `run_id` query param (Phase 14-02); Phase 7 verification doc landed (Phase 15-01).

### Race Page UI (UIRACE)

- [x] **UIRACE-01**: `RacePage.tsx` renders the locked information hierarchy
- [x] **UIRACE-02**: All 12 page states render correctly; WS reconnect resumes from last `turn_index`
- [x] **UIRACE-03**: Visual contract enforced — lane stripe, pill failure-state badge, border-radius scale, banner h1 with 4px primary rule, methodology as flat section, label-above-value ticker
- [x] **UIRACE-04**: `failureTagColor` token map (5 entries) consumed by both heatmap cells and failure-state badges; color paired with icon + label
- [x] **UIRACE-05**: Responsive contract — desktop / tablet / small-tablet / mobile breakpoints
- [x] **UIRACE-06**: Accessibility contract — keyboard nav, WCAG AA contrast, ARIA landmarks, prefers-reduced-motion, prefers-contrast: more, fault_observed via aria-live
- [x] **UIRACE-07**: 8 new glossary terms; first-mention rule extended to Race page

### Heatmap & Replay (HEAT)

- [x] **HEAT-01**: `HardnessFailureHeatmap.tsx` renders rows = HardnessType, columns = lane; cells keyboard-focusable; "directional · n=3 tasks · v1" pill in `secondary.main`
- [x] **HEAT-02**: Heatmap legend strip always visible; footer shows model · seed · pinned task IDs
- [x] **HEAT-03**: Replay route `/race/<run_id>` reads `data/runs/<run_id>.json` (no live LLM); two-layer fixture test (snapshot + `--update-snapshots`)
- [x] **HEAT-04**: K∈{2,3,4,5} sweep on all 3 v1 tasks confirms K=3 produces expected tag (TODO 8 closed)

### OG Image & Sharing (OG)

- [x] **OG-01**: `/race/<run_id>/og.png` Playwright route (1200×630 cropped anchor); cached at `data/og/<run_id>-v<OG_LAYOUT_VERSION>.png`; served via og:image + twitter:image
- [x] **OG-02**: `/race/<run_id>/heatmap.png` Playwright route (1200×900 with annotation strip); shares OG_LAYOUT_VERSION cache key
- [x] **OG-03**: "Copy headline image" button — client-side canvas snapshot fallback if OG generation fails
- [x] **OG-04**: 404 on unknown run_id before Playwright spawn; cleanup task purges stale `<id>-v<old>.*` files when OG_LAYOUT_VERSION bumps

### Tool Discovery (DISC)

- [x] **DISC-01**: New `tool_discovery` scenario in `DemoRepository` exercising MCP tool discovery and A2A agent-card discovery; failure modes include stale capability cache and unknown-tool fallback (Phase 11 code; Phase 16 closed live A2A protocol UAT)
- [x] **DISC-02**: `DiscoveryPhasePanel.tsx` renders the discovery phase as a first-class section above the trace explorer (Phase 11 code; Phase 15 closed W1 gate-inconsistency fix; Phase 16 closed D-72 single-panel + ordering live UAT)

### Visualization (VIZ)

- [x] **VIZ-01**: Annotated diff view between two protocol traces — line-by-line comparison panel that aligns matching events and highlights divergence points
- [x] **VIZ-02**: Interactive sequence diagram (vertical lifelines per actor, horizontal arrows per message) for a single trace; honors `prefers-reduced-motion` and click-to-pin

### Design System Lock (DSGN)

- [x] **DSGN-01**: `.planning/DESIGN.md` formalizing race-demo design tokens — `failureTagColor` map, methodology-as-flat rule, `secondary.main` as replay-pill semantic, role-first first-mention contract, primary/secondary palette intent

---

## Outcome Notes

- **All 31 v2.0 requirements complete** at milestone close (2026-05-04)
- **Gap closure phases 14-16** added post-audit to wire integration (B1/B2/B3), close verification gaps (Phase 7 VERIF, REQUIREMENTS bookkeeping, code cleanup), and run live human UAT for Phase 11 discovery
- **Pivots from initial spec:** None at requirements level. Phase 7 D-14 deferred fault_observed runtime emission to Phase 7 (handled by recovery state machine) — schema-only in Phase 6
- **Promoted from v1.0 backlog:** DISC-01/02, VIZ-01/02, race demo (CEO+eng+design-cleared), TODOs 3/5/8

---

## Deferred to v2.1+

### SDK migrations (SDK)

- **SDK-01**: A2A SDK 0.3.26 → 1.0.0 migration (broker core touch)
- **SDK-02**: MCP SDK v2 migration (`FastMCP` → `McpServer`, server-side rewrite)

### TODOs (carried)

- **TODO-01**: Real plan-emitter hybrid (replace v1 enum tool with `propose_plan` agent step generation)
- **TODO-02**: Multi-seed n=20+ benchmark mode with bootstrap CIs and per-axis winner declaration
- **TODO-04**: Production trace schema migrator
- **TODO-06**: Real display typeface (replace Segoe UI fallback)
- **TODO-07**: HardnessFailureHeatmap auto-renders rows from `HardnessType` enum
- **TODO-09**: HMAC-signed PNG URLs (production hardening)
- **TODO-10**: LLM-judge replacement for `agent_msg_acknowledging_fault` regex (paraphrase resilience)

### v1.0 Process Debt (still carried)

- Phase 5 missing phase-level VERIFICATION.md (bookkeeping)
- Phase 4 — 3 visual verification items (swimlane overlap, compare scroll sync, metrics chip visibility)

---

## Out of Scope (held)

| Feature | Reason |
|---------|--------|
| Dark mode on Race page | Theme is light-only; race demo not the place to introduce a mode toggle |
| Live OpenAI mode on Race page | Race lane locks deterministic mocks with FailureConfig; live LLM is a different product |
| Per-cell heatmap drilldown to full trace | Cell tooltip + sample-trace link is enough; deep drilldown lives in existing TraceExplorer |
| Animation on heatmap cell render | Static grid; animation distracts from "directional" honesty pill |
| Login / saved runs / user-owned share-pages | Anonymous shareable URLs only in v2.0; auth is leaderboard-10x scope (post-v2) |
| Sound / haptic on failure-state badge | Reduced-motion compliance trumps; sound design is a separate phase |
| Per-task "explain why this failure mode" inline copy | Methodology + failure script disclose enough; explanatory prose stays in post body |
| Real plan-emitter hybrid in v2.0 | TODO 1 — agent-driven decision policy is v2.1+ scope |
| Multi-seed statistical benchmark | TODO 2 — conflicts with "no winner declared" rule |
| HMAC URL signing | TODO 9 — hackathon-ephemeral demo; 404-on-unknown-run_id sufficient |
| Production trace migrator | TODO 4 — only matters when an actual v1.0 fixture replay is requested |

---

## Final Traceability (v2.0)

| Requirement | Phase(s) | Status |
|-------------|----------|--------|
| TRC-01 | Phase 6 | Complete |
| TRC-02 | Phase 6 | Complete |
| TRC-03 | Phase 6 | Complete |
| TRC-04 | Phase 6 | Complete |
| RACE-01 | Phase 7 + Phase 14 (integration) | Complete |
| RACE-02 | Phase 7 + Phase 14 (integration) | Complete |
| RACE-03 | Phase 7 + Phase 14 (integration) | Complete |
| RACE-04 | Phase 7 + Phase 14 (integration) | Complete |
| RACE-05 | Phase 7 + Phase 14 (integration) | Complete |
| RACE-06 | Phase 7 + Phase 14 (integration) | Complete |
| RACE-07 | Phase 7 + Phase 14 (integration) | Complete |
| UIRACE-01 | Phase 8 | Complete |
| UIRACE-02 | Phase 8 | Complete |
| UIRACE-03 | Phase 8 | Complete |
| UIRACE-04 | Phase 8 | Complete |
| UIRACE-05 | Phase 8 | Complete |
| UIRACE-06 | Phase 8 | Complete |
| UIRACE-07 | Phase 8 | Complete |
| HEAT-01 | Phase 9 | Complete |
| HEAT-02 | Phase 9 | Complete |
| HEAT-03 | Phase 9 | Complete |
| HEAT-04 | Phase 9 | Complete |
| OG-01 | Phase 10 | Complete |
| OG-02 | Phase 10 | Complete |
| OG-03 | Phase 10 | Complete |
| OG-04 | Phase 10 | Complete |
| DISC-01 | Phase 11 + Phase 16 | Complete |
| DISC-02 | Phase 11 + Phase 15 + Phase 16 | Complete |
| VIZ-01 | Phase 12 | Complete |
| VIZ-02 | Phase 12 | Complete |
| DSGN-01 | Phase 13 | Complete |

**Coverage:** 31/31 v2.0 requirements complete.

---

*Requirements defined: 2026-04-28*
*Milestone closed: 2026-05-04*
