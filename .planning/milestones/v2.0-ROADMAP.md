# Milestone v2.0: Race Demo + Discovery + Visualization

**Status:** ✅ SHIPPED 2026-05-04
**Phases:** 6-16 (11 phases)
**Total Plans:** 52
**Timeline:** 2026-04-28 → 2026-05-04 (7 days)
**Commits:** 261
**Files Changed:** 330 (+63,371 / -230)

## Overview

v2.0 added the **Three-Lane Failure-Shape Race Demo** as the new flagship surface (Phases 6-10), the **Tool Discovery scenario** with first-class `DiscoveryPhasePanel` (Phase 11), **Comparison Visualization upgrades** — annotated diff + interactive sequence diagram (Phase 12), and the **Design System Lock** formalizing race-demo tokens in `DESIGN.md` (Phase 13). Phases 14-16 closed gap-closure work surfaced by the v2.0 milestone audit (B1/B2/B3 race demo wiring, Phase 7 verification doc, REQUIREMENTS bookkeeping, DiscoveryPhasePanel gate W1, Phase 11 human UAT items).

## Phases

### Phase 6: TraceRecorder Schema Gate & Race Foundation

**Goal**: Land the trace + websocket schema upgrades that the rest of v2.0 depends on, closing the design doc's PRE-DESIGN GATE.
**Depends on**: Nothing (first phase of v2.0)
**Requirements**: TRC-01, TRC-02, TRC-03, TRC-04
**Plans**: 8 plans

Plans:

- [x] 06-01: race/ package skeleton + 8 WsEvent dataclasses
- [x] 06-02: race/turn.py TURN_DEFINING_EVENTS dispatch table
- [x] 06-03: TraceRecorder upgrade (schema_version, lane, run_id, turn_index, ndjson hook)
- [x] 06-04: race/failure.py FaultKind + inject_fault IRON RULE + Pydantic validator
- [x] 06-05: race/runs.py RunWriter + threading.Lock single-writer arbiter
- [x] 06-06: race/replay.py stub migrator + path-traversal guard + (run_id, lane) query
- [x] 06-07: race/ws.py ConnectionManager + /api/race/ws full lifecycle route
- [x] 06-08: tests/race/ test suite (8 files; field-presence, ndjson, IRON RULE, ws lifecycle)

**Details:** Closed PRE-DESIGN GATE. Locked TraceRecorder additive extension preserving v1 backwards-compat. NEVER_COALESCE has exactly 7 members; only TickEvent eligible for coalescing. IRON RULE atomicity — fault_injected recorded BEFORE mutation, even on raise paths.

### Phase 7: Race Backend — Lanes, Harness, Recovery State Machine

**Goal**: Stand up the three runner lanes, the harness that drives parallel runs, the locked recovery state machine, and the three v1 tasks with their mock APIs.
**Depends on**: Phase 6
**Requirements**: RACE-01, RACE-02, RACE-03, RACE-04, RACE-05, RACE-06, RACE-07
**Plans**: 11 across 7 waves (W0-W6)

Plans:

- [x] 07-01: race/failure.py InjectedFaultError + anthropic/pyyaml deps (W0)
- [x] 07-02: race/classifier.py Detector(K=3) + agent_msg_acknowledging_fault regex (W1)
- [x] 07-03: race/scorers.py + Haiku judge integration (W2)
- [x] 07-04: race/tasks/ task_config.yaml schema + Pydantic validators (W3)
- [x] 07-05: race/mocks/ GitHub + calendar + travel mock APIs (W3)
- [x] 07-06: race/runners/pure_mcp.py + pure_a2a.py + hybrid.py (W4)
- [x] 07-07: failure_mode_classifier 6-template headline sentences (W5)
- [x] 07-08: race/harness.py n=5/n=1 driver + transient retry policy (W5)
- [x] 07-09: HardnessProfile validator across 3 v1 tasks (W6)
- [x] 07-10: tests/race/test_classifier_calibration.py (W6)
- [x] 07-11: tests/race/test_harness_integration.py (W6)

**Details:** Recovery state machine K=3 confirmed. Multi-task calibration promoted from TODO 8. Per-task callable registries (TARGETS + BINDS); Pydantic startup validation. Harness only retries transient infrastructure errors (not injected faults). Phase 7 verification doc landed retroactively in Phase 15 (07-VERIFICATION.md).

### Phase 8: Race Page UI & Visual Contract

**Goal**: Deliver the three-lane race page that renders the locked information hierarchy, the full set of 12 page states, and the visual / responsive / accessibility contracts.
**Depends on**: Phase 6, Phase 7
**Requirements**: UIRACE-01, UIRACE-02, UIRACE-03, UIRACE-04, UIRACE-05, UIRACE-06, UIRACE-07
**Plans**: 7 across 4 waves

Plans:

- [x] 08-01: Tokens + glossary + FirstMentionProvider + GlossaryTerm Popover branch + shared race types (W1)
- [x] 08-02: Routes (/race + /race/:run_id) + AppShell nav + RacePage shell + 12-state derivePageState (W2)
- [x] 08-03: TDD raceReducer + useRaceStream(enabled) + useRaceReplay (W3)
- [x] 08-04a: Lane/badge family — RaceLaneCard + RaceLaneTicker + FailureStateBadge + ReplayPill (W3)
- [x] 08-04b: Chrome family — RaceStatusStrip + CharacteristicFailureBanner + MethodologySection (W3)
- [x] 08-05: HeatmapScaffold + ReplayScrubber (W3)
- [x] 08-06: RacePage integration + 12-state fixtures + responsive + a11y suite (W4)

**Details:** 8 new race glossary terms with first-mention popovers. failureTagColor map (5 entries) is single source of truth. Visual contract enforced — lane stripes, pill badges, border-radius scale (lane=18 / banner=0 / badge=4 / heatmap-cell=0 / pills=999). 4 responsive breakpoints; full a11y (Tab order, focus-visible, aria-live, prefers-reduced-motion, prefers-contrast).

### Phase 9: Heatmap, Replay & K=3 Calibration

**Goal**: Ship the hardness-vs-failure heatmap as the closing artifact, the deterministic `/race/<run_id>` replay path, and lock K=3 across all three v1 tasks.
**Depends on**: Phase 7, Phase 8
**Requirements**: HEAT-01, HEAT-02, HEAT-03, HEAT-04
**Plans**: 4 across 2 waves

Plans:

- [x] 09-01: race/config.py HEATMAP_BASELINE + race/heatmap.py aggregator + cache + GET /api/race/heatmap + harness run_meta + invalidate_cache hook (W1)
- [x] 09-02: GET /api/race/runs/{run_id}/trace replay route + 400/404/schema tests (W1)
- [x] 09-03: pytest --update-snapshots + replay_with_k + test_replay_symmetry + tests/test_recovery_calibration.py K∈{2,3,4,5} sweep (W1)
- [x] 09-04: HardnessFailureHeatmap.tsx + useRaceHeatmap hook + fetchRaceHeatmap client + RacePage wiring (W2)

**Details:** TODO 8 (multi-task K=3 calibration) closed. Frozen-dataclass HEATMAP_BASELINE with pinned-baseline filter. run_meta is FIRST per-run trace event. invalidate_cache hook fired immediately after race_done.

### Phase 10: OG Image & Sharing

**Goal**: Make every `/race/<run_id>` URL shareable with server-rendered OG and heatmap PNGs, with a client-side fallback.
**Depends on**: Phase 8, Phase 9
**Requirements**: OG-01, OG-02, OG-03, OG-04
**Plans**: 5 across 4 waves

Plans:

- [x] 10-01: race/og.py module (Playwright lifespan + asyncio.Lock + cache helpers) + OG_LAYOUT_VERSION + .gitignore + pyproject optional-dep + cache unit tests (W1)
- [x] 10-02: web.py lifespan registration + /race + /race/{run_id} HTML route w/ meta-tag injection + /race/{run_id}/og.png + /race/{run_id}/heatmap.png + 9-test D-63 mocked-render matrix (W2)
- [x] 10-03: RacePage `?og=1` mode + data-og-anchor/data-og-ready/data-heatmap-anchor sentinels + WS-gating + HardnessFailureHeatmap ogAnnotation prop + HeatmapAnnotationStrip (W2)
- [x] 10-04: html2canvas dep + CopyHeadlineImageButton (lazy import + ClipboardItem + download fallback) + CharacteristicFailureBanner actionSlot prop + RacePage button mount + 4 vitest cases (W3)
- [x] 10-05: Phase 8 mobile-summary placeholder closure: <img src=/race/{run_id}/og.png> consumer w/ onError graceful degradation (W4)

**Details:** TODO 3 (OG image gen) closed. Playwright headless chromium singleton via FastAPI lifespan + asyncio.Lock for cross-request render serialization. Disk-backed cache with deterministic filename and lazy version-mismatch purge. Client-side canvas snapshot fallback if server OG generation fails.

### Phase 11: Tool Discovery Scenario

**Goal**: Surface MCP tool discovery and A2A agent-card discovery as a first-class UI section above the trace explorer, on a dedicated scenario.
**Depends on**: Phase 6 (trace schema)
**Requirements**: DISC-01, DISC-02
**Plans**: 4 across 3 waves

Plans:

- [x] 11-01: Wave 0 refactor — extract JsonTree + FIELD_ANNOTATIONS + annotate from ProtocolEnvelopeDrawer to lib/trace/JsonTree.tsx (DISC-02)
- [x] 11-02: Wave 1 backend — TICKET-1013 + CUST-005 seed rows + pytest coverage proving discovery+fallback emission (DISC-01)
- [x] 11-03: Wave 1 frontend — DiscoveryPhasePanel.tsx + DiscoveryPhasePanelProps + 5 vitest cases (incl. a2a_remote_discovery skill chips) (DISC-02)
- [x] 11-04: Wave 2 mount-site wiring — TraceWorkspacePage gate (D-73) + CompareTracesPanel single-panel above dual-column (D-72) + integration verification (DISC-02)

**Details:** Failure modes reuse existing tool_transport_fallback event — no new event_type. A2A column unions tool_discovery (with remote_agent) AND a2a_remote_discovery (joins by agent_id). Decisions D-67..D-73 locked discovery panel mounting policy. Phase 11 human UAT (live MCP + A2A runs, D-72 layout, ordering) closed by Phase 16.

### Phase 12: Comparison Visualization Upgrades

**Goal**: Deepen the existing comparison story with annotated diff between two protocol traces and an interactive sequence diagram for a single trace.
**Depends on**: Phase 6 (trace schema)
**Requirements**: VIZ-01, VIZ-02
**Plans**: 3 across 2 waves

Plans:

- [x] 12-01: alignTraces pure function + vitest (`frontend/src/components/traces/diffAlign.ts`) — VIZ-01 algorithm foundation (W1)
- [x] 12-02: SequenceDiagramView + TraceExplorer toggle (List|Sequence) + vitest — closes VIZ-02 (W1)
- [x] 12-03: AnnotatedDiffView + CompareTracesPanel toggle (Side-by-side|Annotated diff) + vitest — closes VIZ-01 (W2)

**Details:** D-78 role-first labels via `traceLabel()`. D-84 reuse existing tokens (failureTagColor, getProtocolColor, MUI palette) — no new colors. D-85 zero new dependencies — no @xyflow/react, no motion library. Hand-rolled SVG sequence diagram with 5 fixed lifelines, click-to-pin, prefers-reduced-motion honoured.

### Phase 13: Design System Lock

**Goal**: Run `/design-consultation` against the now-shipped race demo and produce `.planning/DESIGN.md` formalizing the new design tokens and rules.
**Depends on**: Phase 8, Phase 9, Phase 10
**Requirements**: DSGN-01
**Plans**: 1 plan

Plans:

- [x] 13-01: /design-consultation + author .planning/DESIGN.md (5 mandated items: failureTagColor table, methodology-as-flat, secondary.main semantic, role-first contract, palette intent) (W1)

**Details:** TODO 5 closed. DESIGN.md (158 lines) codifies failureTagColor map (5 entries), methodology-as-flat-section rule, secondary.main as replay-pill semantic, role-first first-mention contract, primary/secondary palette intent.

### Phase 14: Race Demo Integration Fix (Gap Closure)

**Goal**: Make the race demo functionally end-to-end — POST /api/race/run trigger, WS run_id routing, heatmap_has_data wiring, ReplayScrubber seek.
**Depends on**: Phase 7, Phase 8, Phase 9
**Gap Closure**: B1 (no HTTP race trigger), B2 (WS run_id mismatch), B3 (heatmap_has_data hardcoded), W2 (scrubber no-op)
**Requirements**: RACE-01..07 (integration), TRC-04, UIRACE-01..03, HEAT-01..03
**Plans**: 4 plans

Plans:

- [x] 14-01: POST /api/race/run endpoint — generate run_id, call run_race(ws_emitter=MANAGER.publish) as background task
- [x] 14-02: Fix useRaceStream.ts buildWsUrl() — receive run_id prop, append to WS URL; RacePage fetches run_id before WS open
- [x] 14-03: Wire useRaceHeatmap() data into heatmap_has_data in RacePage.tsx
- [x] 14-04: Wire ReplayScrubber.onScrub — implement turn-index seek in useRaceReplay

**Details:** Three audit blockers (B1/B2/B3) plus W2 closed. Backend 352/352 pytest, frontend 335/335 vitest. Human UAT approved 2026-05-04.

### Phase 15: Verification & Cleanup (Gap Closure)

**Goal**: Close documentation and code-quality gaps from the audit — write Phase 7 VERIFICATION.md, fix DiscoveryPhasePanel gate W1, fix REQUIREMENTS.md stale checkboxes, clean up Phase 12 code-quality.
**Depends on**: Phase 7, Phase 11, Phase 12
**Gap Closure**: Phase 7 missing VERIFICATION.md, W1 (TraceWorkspacePage gate), REQUIREMENTS.md stale checkboxes, W-VERIF-1..3
**Requirements**: RACE-01..07 (verification doc), DISC-02 (gate fix)
**Plans**: 4 plans

Plans:

- [x] 15-01: Write 07-VERIFICATION.md aggregating Phase 7 SUMMARY evidence + RACE-01..07 closure
- [x] 15-02: Fix TraceWorkspacePage DiscoveryPhasePanel gate — replace scenario-string check with event-presence check matching CompareTracesPanel
- [x] 15-03: REQUIREMENTS.md traceability verification (verified correct, no changes needed)
- [x] 15-04: Phase 12 cleanup — remove duplicate import, fix D-83 reduced-motion test, remove dead pinnedEventId prop from ProtocolTier

**Details:** All 4 audit cleanup tasks closed. Frontend 335/335 vitest after gate fix. Two-gate inconsistency (TraceWorkspacePage scenario-string vs CompareTracesPanel event-presence) resolved.

### Phase 16: Discovery UAT (Gap Closure)

**Goal**: Complete the 4 outstanding human verification items from Phase 11 by running tool_discovery scenario live in MCP + A2A modes, confirming D-72 single-panel layout, ordering, and stale-cache UX.
**Depends on**: Phase 11, Phase 14
**Gap Closure**: W3 (Phase 11 human verification items)
**Requirements**: DISC-01, DISC-02
**Plans**: 1 plan

Plans:

- [x] 16-01: Live UAT — run tool_discovery in MCP + A2A mode, verify 4 human items, update 11-VERIFICATION.md status

**Details:** All 4 human checks passed. 11-VERIFICATION.md status flipped from `human_needed` to `passed`. Bonus fix landed during UAT: ReportService.save_report now merges protocol runs instead of overwriting (compare-mode regression discovered live).

---

## Milestone Summary

**Decimal Phases:** None — all v2.0 phases sequential (6-16).

**Key Decisions:**

- Phase 6 PRE-DESIGN GATE first — blocked all downstream race work (front-loaded by design)
- Hybrid runner consolidated into Phase 7 with recovery state machine — risk consolidated, not spread
- DSGN-01 deliberately last (Phase 13) — `/design-consultation` benefits from race-demo design rules already concrete in code
- failureTagColor as single source of truth across heatmap + badges
- Three audit-discovered blockers (B1/B2/B3) accepted as gap-closure phases (14-16) rather than re-opening completed phases — cleaner traceability, separate verification surface
- Phase 16 UAT bonus fix (save_report merge) committed live during human UAT — pragmatic over process

**Issues Resolved:**

- B1: run_race() never called from web.py — fixed by Phase 14-01 (POST /api/race/run)
- B2: useRaceStream missing run_id query param — fixed by Phase 14-02
- B3: heatmap_has_data hardcoded false — fixed by Phase 14-03
- W1: TraceWorkspacePage scenario-string gate inconsistent with CompareTracesPanel event-presence — fixed by Phase 15-02
- W2: ReplayScrubber.onScrub no-op — fixed by Phase 14-04
- W3: Phase 11 human UAT items unclosed — closed by Phase 16-01
- W-VERIF-1..3: duplicate import, reduced-motion test, dead pinnedEventId prop — closed by Phase 15-04
- Phase 7 missing VERIFICATION.md — closed by Phase 15-01
- ReportService.save_report compare-mode protocol overwrite — fixed inline during Phase 16 UAT

**Issues Deferred (carried to v2.1+):**

- SDK-01: A2A SDK 0.3.26 → 1.0.0 migration (broker core touch)
- SDK-02: MCP SDK v2 (`FastMCP` → `McpServer`) migration
- TODO-01: Real plan-emitter hybrid (replace v1 enum tool with `propose_plan` agent step generation)
- TODO-02: Multi-seed n=20+ benchmark mode with bootstrap CIs
- TODO-04: Production trace schema migrator (real transformations of v1.0 fixtures)
- TODO-06: Real display typeface
- TODO-07: HardnessFailureHeatmap auto-renders rows from HardnessType enum
- TODO-09: HMAC-signed PNG URLs (production hardening)
- TODO-10: LLM-judge replacement for `agent_msg_acknowledging_fault` regex

**Technical Debt Incurred:**

- `api.ts` + `api.generated.ts` dual-patching tax persists from v1.0 (manual edits in both files for new fields)
- `tool_transport_fallback` event type doubles as discovery-failure signal — pragmatic but coupled

---

*For current project status, see .planning/ROADMAP.md*
