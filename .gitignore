.venv/
__pycache__/
*.py[cod]

artifacts/
data/

# Phase 6 — race demo run trace files (data/runs/<run_id>.json append-only ndjson)
/data/runs/

# Phase 7 — race fixture data is checked-in (read-only JSON for mocks)
# Re-include only the race/fixtures subtree under data/. Other data/* paths
# (data/runs, data/seeds, etc.) remain ignored.
!/data/
/data/*
!/data/race/
/data/race/*
!/data/race/fixtures/
!/data/race/fixtures/**

# Phase 10 — OG image disk cache (regenerated on demand)
data/og/*.png

frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo

.env

# Build artifacts
*.egg-info/
*.egg

# Temp files
.tmp/

# Claude Code local config
.claude/

# OS
.DS_Store
Thumbs.db

# graphify cache (regenerable AST extraction artifacts)
graphify-out/cache/
